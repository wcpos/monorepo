# Template Studio snapshot summary

Curated snapshots: 2 templates × 7 seeded scenarios × 1 widths.
Each scenario uses a deterministic seed plus explicit overrides so randomizer drift is not silently absorbed.
