diff --git a/dist/cjs/incremental-write.js b/dist/cjs/incremental-write.js
index 2faeee563005b7574168b4a071de2e4aa273df82..217251beffe25cc63b7edec85430e4f376958c86 100644
--- a/dist/cjs/incremental-write.js
+++ b/dist/cjs/incremental-write.js
@@ -49,13 +49,15 @@ var IncrementalWriteQueue = exports.IncrementalWriteQueue = /*#__PURE__*/functio
       return;
     }
     this.isRunning = true;
+    var itemsById;
+    try {
     var writeRows = [];
 
     /**
      * 'take over' so that while the async functions runs,
      * new incremental updates could be added from the outside.
      */
-    var itemsById = this.queueByDocId;
+    itemsById = this.queueByDocId;
     this.queueByDocId = new Map();
     await Promise.all(Array.from(itemsById.entries()).map(async ([_docId, items]) => {
       var oldData = findNewestOfDocumentStates(items.map(i => i.lastKnownDocumentState));
@@ -125,7 +127,20 @@ var IncrementalWriteQueue = exports.IncrementalWriteQueue = /*#__PURE__*/functio
         items.forEach(item => item.reject(rxError));
       }
     });
-    this.isRunning = false;
+    } catch (err) {
+      // A rejected storage bulkWrite (or a throw in result processing) used
+      // to leave isRunning=true forever, silently wedging every later
+      // incremental write on this queue. Reject the taken-over items so
+      // callers observe the failure. https://github.com/pubkey/rxdb/pull/9026
+      if (itemsById) {
+        Array.from(itemsById.values()).forEach(function (items) {
+          items.forEach(function (item) { item.reject(err); });
+        });
+      }
+    } finally {
+      this.isRunning = false;
+    }
+    
 
     /**
      * Always trigger another run
diff --git a/dist/esm/incremental-write.js b/dist/esm/incremental-write.js
index 700e212893db33bd301fc15837b92d1e3d1c4fc5..4104b80f264e79c8624b744788a13c8401cd2448 100644
--- a/dist/esm/incremental-write.js
+++ b/dist/esm/incremental-write.js
@@ -41,13 +41,15 @@ export var IncrementalWriteQueue = /*#__PURE__*/function () {
       return;
     }
     this.isRunning = true;
+    var itemsById;
+    try {
     var writeRows = [];
 
     /**
      * 'take over' so that while the async functions runs,
      * new incremental updates could be added from the outside.
      */
-    var itemsById = this.queueByDocId;
+    itemsById = this.queueByDocId;
     this.queueByDocId = new Map();
     await Promise.all(Array.from(itemsById.entries()).map(async ([_docId, items]) => {
       var oldData = findNewestOfDocumentStates(items.map(i => i.lastKnownDocumentState));
@@ -117,7 +119,20 @@ export var IncrementalWriteQueue = /*#__PURE__*/function () {
         items.forEach(item => item.reject(rxError));
       }
     });
-    this.isRunning = false;
+    } catch (err) {
+      // A rejected storage bulkWrite (or a throw in result processing) used
+      // to leave isRunning=true forever, silently wedging every later
+      // incremental write on this queue. Reject the taken-over items so
+      // callers observe the failure. https://github.com/pubkey/rxdb/pull/9026
+      if (itemsById) {
+        Array.from(itemsById.values()).forEach(function (items) {
+          items.forEach(function (item) { item.reject(err); });
+        });
+      }
+    } finally {
+      this.isRunning = false;
+    }
+    
 
     /**
      * Always trigger another run
