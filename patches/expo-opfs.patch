diff --git a/dist/src/index.js b/dist/src/index.js
index ceec5ec8e280c4e1a8e7498b454171d44cbcca6f..408bfe44c9ae37624b8bd56b2b66cb2a9cd4b272 100644
--- a/dist/src/index.js
+++ b/dist/src/index.js
@@ -450,20 +450,15 @@ class FileSystemWritableFileStream {
         try {
             if (this.fileHandle) {
                 this.fileHandle.close();
-                // Atomic commit: move swap file over the original file
+                // Atomic commit: move swap file over the original file.
+                // Use File.move() instead of read+write to minimize the
+                // window between delete and replace — prevents data
+                // corruption when rxdb-premium closes the stream after
+                // every write operation.
                 if (this.fileNode.exists) {
                     this.fileNode.delete();
                 }
-                // Workaround for expo-file-system lack of synchronous move:
-                // Read swap file completely and write to the target natively
-                const swapHandle = this.swapNode.open();
-                const finalData = swapHandle.readBytes(swapHandle.size ?? 0);
-                swapHandle.close();
-                this.fileNode.create();
-                const finalHandle = this.fileNode.open();
-                finalHandle.writeBytes(finalData);
-                finalHandle.close();
-                this.swapNode.delete();
+                this.swapNode.move(this.fileNode);
             }
             else {
                 // Zero-byte file that was created and immediately closed
diff --git a/src/index.ts b/src/index.ts
index b27d3e71e3e4f6fe927c57db268acba23d9e86db..24cd14ab629f5bfdfe126fc54e639227549a4300 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -491,22 +491,15 @@ export class FileSystemWritableFileStream {
             if (this.fileHandle) {
                 this.fileHandle.close();
 
-                // Atomic commit: move swap file over the original file
+                // Atomic commit: move swap file over the original file.
+                // Use File.move() instead of read+write to minimize the
+                // window between delete and replace — prevents data
+                // corruption when rxdb-premium closes the stream after
+                // every write operation.
                 if (this.fileNode.exists) {
                     this.fileNode.delete();
                 }
-                // Workaround for expo-file-system lack of synchronous move:
-                // Read swap file completely and write to the target natively
-                const swapHandle = this.swapNode.open();
-                const finalData = swapHandle.readBytes(swapHandle.size ?? 0);
-                swapHandle.close();
-
-                this.fileNode.create();
-                const finalHandle = this.fileNode.open();
-                finalHandle.writeBytes(finalData);
-                finalHandle.close();
-
-                this.swapNode.delete();
+                this.swapNode.move(this.fileNode);
             } else {
                 // Zero-byte file that was created and immediately closed
                 if (!this.fileNode.exists) {
