node_modules
.env
.vscode
.DS_Store
.turbo
.translations
.worktrees
# Ignore local Claude runtime state, but keep project rules, skills and the
# shared permission policy tracked. settings.local.json stays ignored — it is
# per-machine, where settings.json is the policy everyone works under.
.claude/*
!.claude/rules/
!.claude/rules/**
!.claude/skills/
!.claude/skills/**
!.claude/settings.json
docs/
apps/main/web-build-atlas/

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# Coverage
coverage/
**/coverage/

# Ignore .cursor except rules (needed for AI agents)
.cursor/*
!.cursor/rules/
docs/
# Local scratch (Paul) — real store data / local wiki checkout, never commit
/paper-current.html
/.wiki/

# Local native E2E runs (video + logs, ~15MB each)
.e2e-local/
