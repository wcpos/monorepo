# Behaviour ledger: `auth/components`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/connect/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Present saved sites, account authentication and validated store selection, plus URL and Demo entry.

**Composes:** `@wcpos/components` accordion, alert-dialog, avatar, button, card, docs-link, error-boundary, hstack, icon, icon-button, input, label, list-item, loader, radio-group, status-badge, suspense, text and vstack; auth discovery/login hooks, `useWcposAuth`, `useUserValidation` and `useRegister`.

## Lines

1. Sites: First site expands initially; newly added site expands immediately; explicit all-collapsed state survives ordinary updates; removal of the expanded site falls back to the first remaining site — evidence: `fb82816316 2026-06-08 Expand newly added connect site`, `9b18f98e67 2026-06-08 Fix collapsed site expansion selection`, code: `"const addedSiteUuid = currentSiteUuids.find((uuid) => !previousSiteUuids.includes(uuid));"` in `packages/core/src/screens/auth/components/sites-expansion.ts:15`, code: `"if (currentExpandedSiteUuid === '') {"` in `packages/core/src/screens/auth/components/sites-expansion.ts:20`, code: `"return currentSiteUuids[0] ?? '';"` in `packages/core/src/screens/auth/components/sites-expansion.ts:28` — platform: all.
2. Redirect return: Expand the initiating site so its otherwise-unmounted authentication consumer can receive the one-shot result — evidence: `511a28cf7b 2026-08-20 fix(core): claim redirect-return logins per initiating consumer; harden CSRF and expansion`, code: `"const pendingLoginUrl = peekRedirectLoginUrl();"` in `packages/core/src/screens/auth/components/sites.tsx:62`, code: `"return getInitialAccordionState(siteUuids, pendingSiteUuid);"` in `packages/core/src/screens/auth/components/sites.tsx:66`, test “expands the preferred site (pending redirect-return login) instead” — platform: web.
3. URL entry: Clear refocuses; editing dismisses stale errors; keyboard submission runs the same connect operation — evidence: `480571d161 2026-04-14 fix(auth): display connection errors and handle restricted REST APIs`, `c3224012a7 2024-10-10 update UI (WIP)`, code: `"if (error) reset();"` in `packages/core/src/screens/auth/components/url-input.tsx:33`, code: `"onSubmitEditing={() => onConnect(url)}"` in `packages/core/src/screens/auth/components/url-input.tsx:35`; see `packages/components/src/input/LEDGER.md` line 4 — platform: all.
4. User selection: First credential is implicit default; removed explicit choice falls back to first remaining; changing user resets store choice — evidence: `70ac133f6e 2026-05-22 fix(deps): resolve react-hooks v7 violations + jest 30 test stack for SDK 56`, `f5e4f50bc7 2026-04-17 feat(auth): redesign connect screen and harden store/user sync`, code: `"(pickedUserUuid ? wpCreds.find((c) => c.uuid === pickedUserUuid) : undefined) ?? wpCreds[0];"` in `packages/core/src/screens/auth/components/wp-users.tsx:45`, code: `"setSelectedStoreId(null);"` in `packages/core/src/screens/auth/components/wp-users.tsx:68` — platform: all.
5. User row: Re-authenticate and remove stop press propagation; selected expired users keep selected styling instead of being visually replaced by the warning variant — evidence: `f5e4f50bc7 2026-04-17 feat(auth): redesign connect screen and harden store/user sync`, `27f87edd4f 2026-04-21 fix(auth): clear authFailed after reauth and scope warning variant`, code: `"e.stopPropagation();"` in `packages/core/src/screens/auth/components/wp-user.tsx:144`, code: `"variant={showReauth && !isSelected ? 'warning' : undefined}"` in `packages/core/src/screens/auth/components/wp-user.tsx:169`; see `packages/components/src/list-item/LEDGER.md` lines 1, 2 — platform: all.
6. Account addition: New presses reset response deduplication so repeating the same login/error is not silently ignored — evidence: `2766e9cba6 2026-04-21 fix(auth): address review feedback for connect screen hardening`, code: `"processedResponseRef.current = null;"` in `packages/core/src/screens/auth/components/add-user-button.tsx:75` — platform: all.
7. Re-authentication: A returned different user may be added, but their token must not replace the selected user’s active token; warn explicitly — evidence: `511a28cf7b 2026-08-20 fix(core): claim redirect-return logins per initiating consumer; harden CSRF and expansion`, code: `"await handleLoginSuccess({ params });"` in `packages/core/src/screens/auth/components/wp-user.tsx:89`, code: `"if (Number.parseInt(params.id, 10) === Number(wpUser.id)) {"` in `packages/core/src/screens/auth/components/wp-user.tsx:96`, code: `"'Re-authentication returned a different user; credentials saved but token not adopted for active requests',"` in `packages/core/src/screens/auth/components/wp-user.tsx:101` — platform: all.
8. Store list: Names sort naturally and case-insensitively; numeric store ID breaks name ties, avoiding hash-order reshuffling between logins — evidence: `a167dbd4fb 2026-08-10 fix(auth): give the store picker a stable, human order`, code: `"const storeCollator = new Intl.Collator(undefined, { numeric: true, sensitivity: 'base' });"` in `packages/core/src/screens/auth/components/store-select.tsx:24`, code: `"return (Number(a.id) || 0) - (Number(b.id) || 0);"` in `packages/core/src/screens/auth/components/store-select.tsx:42` — platform: all.
9. Store refresh: Reactively inserted/renamed stores and newly received numeric IDs update the picker; switching users clears previous stores — evidence: `f5e4f50bc7 2026-04-17 feat(auth): redesign connect screen and harden store/user sync`, `dd98ccef6e 2026-04-21 fix(auth): gate Open POS on validated credentials and reset stores on user switch`, `e544911176 2026-08-16 fix(ui): subscribe to the fields that are rendered, instead of reading them off the document (#1240)`, code: `"return storesCollection.find({ selector: { localID: { $in: validIds } } })"` in `packages/core/src/screens/auth/components/store-select.tsx:113`, code: `"startWith([] as StoreDocument[])"` in `packages/core/src/screens/auth/components/store-select.tsx:128`, code: `"store.name === b[index].name &&"` in `packages/core/src/screens/auth/components/store-select.helpers.ts:20`, code: `"store.id === b[index].id"` in `packages/core/src/screens/auth/components/store-select.helpers.ts:21` — platform: all.
10. Register binding: A binding only preselects a store actually offered to this user; unavailable bindings do not remove the ordinary picker; lone-store behavior remains — evidence: `155a6a5979 2026-09-11 feat(auth): a store-bound register skips the store picker at sign-in (wcpos/roadmap#242)`, wcpos/roadmap#242, code: `"if (stores.length === 1) return stores[0];"` in `packages/core/src/screens/auth/components/store-select.helpers.ts:31`, code: `"stores.find((store) => typeof boundStoreId === 'number' && store.id === boundStoreId) ?? null"` in `packages/core/src/screens/auth/components/store-select.helpers.ts:33`, test “shows the picker when the binding is not offered” — platform: all.
11. Validation: Known invalid/in-flight users cannot open POS; background-blocked validation defers and retries on wake rather than immediately marking credentials invalid — evidence: `dd98ccef6e 2026-04-21 fix(auth): gate Open POS on validated credentials and reset stores on user switch`, `9fb2b51499 2026-08-18 fix(auth): defer user validation while the tab is hidden instead of logging AUTH999 errors`, code: `"const canLogin = !!selectedStoreLocalID && isValid && !isValidating;"` in `packages/core/src/screens/auth/components/store-select.tsx:160`, code: `"if (isAsleepBlock(error)) {"` in `packages/core/src/hooks/use-user-validation.ts:404`, code: `"lastValidationKey.current = '';"` in `packages/core/src/hooks/use-user-validation.ts:407` — platform: all.
12. Demo: Discovery precedes auth; one auth prompt per attempt; Demo does not bypass the store-selection flow — evidence: `ab3dea833c 2025-08-08 update demo button for v1.8-beta`, `311f04cb11 2025-12-04 fix auth flow for electron and web`, code: `"const site = await onConnect('https://demo.wcpos.com');"` in `packages/core/src/screens/auth/components/demo-button.tsx:78`, code: `"if (authTriggeredRef.current || processedResponseRef.current !== null) {"` in `packages/core/src/screens/auth/components/demo-button.tsx:101`, code: `"if (connectedSite && isReady && !isProcessing) {"` in `packages/core/src/screens/auth/components/demo-button.tsx:105`, code: `"authTriggeredRef.current = true; // Set immediately before async call"` in `packages/core/src/screens/auth/components/demo-button.tsx:107`, code: `"void handleLoginSuccess({ params: response.params } as any);"` in `packages/core/src/screens/auth/components/demo-button.tsx:58`, code: `"if (router.canGoBack()) {"` in `packages/core/src/screens/auth/hooks/use-login-handler.ts:137` — platform: all.
13. Extends line 2: Claim redirect-return results per initiating consumer, separating add-user from each re-auth row so one consumer cannot swallow another’s one-shot login — evidence: `511a28cf7b 2026-08-20 fix(core): claim redirect-return logins per initiating consumer; harden CSRF and expansion`, code: `"claimKey: 'add-user',"` in `packages/core/src/screens/auth/components/add-user-button.tsx:33`, code: ``"claimKey: `reauth:${wpUser.uuid}`,"`` in `packages/core/src/screens/auth/components/wp-user.tsx:49` — platform: web. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
14. Extends line 7: After the login handler returns for the same row’s user, adopt the refreshed token and clear the pre-flight auth-failed flag so subsequent requests are not rejected despite newly saved credentials — evidence: `27f87edd4f 2026-04-21 fix(auth): clear authFailed after reauth and scope warning variant`, `511a28cf7b 2026-08-20 fix(core): claim redirect-return logins per initiating consumer; harden CSRF and expansion`, code: `"if (Number.parseInt(params.id, 10) === Number(wpUser.id)) {"` in `packages/core/src/screens/auth/components/wp-user.tsx:96`, code: `"requestStateManager.setRefreshedToken(params.access_token);"` in `packages/core/src/screens/auth/components/wp-user.tsx:97`, code: `"requestStateManager.setAuthFailed(false);"` in `packages/core/src/screens/auth/components/wp-user.tsx:98` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
15. Show a coded documentation link below the connect error and carry the code in its testID — translated copy and the web DocsLink’s lack of a DOM href must not prevent identifying which condition surfaced — evidence: `14b2455cf8 2026-08-22 feat(core,utils): name the host-blocked auth and transport conditions (Package C, step 1)`, `22db3b5364 2026-08-22 fix(e2e,core): assert the coded connect error by a code-bearing testID`, code: ``"testID={`connect-error-docs-link-${errorCode}`}"`` in `packages/core/src/screens/auth/components/url-input.tsx:61`, test “renders the coded connect error documentation link directly below the error” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
16. Address store options by WooCommerce store ID, not the credential-dependent localID hash, so tests select the intended store rather than a random first row — evidence: `73efaae7e6 2026-08-10 test(e2e): let specs choose their store, and run coupon parity against every one`, woocommerce-pos#1548, code: ``"testID={store.id != null ? `store-option-${store.id}` : undefined}"`` in `packages/core/src/screens/auth/components/store-select.tsx:190` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
17. Extends line 10: Resolve the explicit store pick against currently offered stores and use that same effective selection for highlight, radio and login — a stale pick must fall away and a missing store ID must never reach onLogin — evidence: `802a4f5ae1 2026-08-29 refactor(core): remove four effects that only existed to sync state (#1673)`, code: `"stores.find((s) => s.localID === selectedStoreId) ?? (stores.length === 1 ? stores[0] : null),"` in `packages/core/src/screens/auth/components/store-select.tsx:151`, code: `"if (selectedStoreLocalID) onLogin(selectedStoreLocalID);"` in `packages/core/src/screens/auth/components/store-select.tsx:220` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
18. Log a rejected store login rather than leaving the cashier on the picker with no diagnostic feedback — evidence: `152e49b4e1 2026-08-19 refactor(database,core): clear the fastStoreDB residue and type useAppState`, code: `"authLogger.error('Store login failed', {"` in `packages/core/src/screens/auth/components/wp-users.tsx:96` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
19. Extends line 12: Do not add a second toast for a Demo connection failure because discovery and authorization hooks already display the specific error — evidence: `311f04cb11 2025-12-04 fix auth flow for electron and web`, code: `"// Don't show toast here - specific error messages are already displayed"` in `packages/core/src/screens/auth/components/demo-button.tsx:88`, code: ``"authLogger.error(`Demo connection failed: ${getErrorMessage(err)}`, {"`` in `packages/core/src/screens/auth/components/demo-button.tsx:90` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
20. Extends line 7: Show a translated different-user warning rather than exposing the diagnostic log message in the merchant’s toast — evidence: `ed0b42e11a 2026-08-20 fix(core): show a translated toast when re-authentication returns a different account`, code: `"toast: { title: t('auth.signed_in_as_different_user', { _tags: 'core' }) },"` in `packages/core/src/screens/auth/components/wp-user.tsx:104`, test “shows a translated toast without exposing diagnostics when a different user returns” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
