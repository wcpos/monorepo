# Behaviour ledger: `auth/hooks`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/connect/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Discover and validate a site, preserve its local account links, and save returned login credentials.

**Composes:** `useHttpClient`, app-state authorization/compatibility probes, site-write helpers and the credentials database; no visual components.

## Lines

1. Site reconnect: Updating discovery details preserves locally linked users rather than erasing them while login is in flight — evidence: `aff66aa2d8 2026-08-06 fix(auth): atomic wp_credentials link — login no longer races connect's site writes (#902) (#1013)`, issue #902, PR #1013, code: ``"* This must not be a full-document write: `parsedData` carries the schema"`` in `packages/core/src/screens/auth/hooks/use-site-connect.ts:153`, code: `"const siteDoc = await upsertSiteData(userDB.sites, parsedData);"` in `packages/core/src/screens/auth/hooks/use-site-connect.ts:158` — platform: all.
2. Discovery: Front-page timeout still allows `/wp-json/` fallback; a challenge on the front page alone does not prevent a successful fallback — evidence: `c7242dc18b 2026-08-12 fix(auth): bound store-discovery probes so a hung server can't spin Connect forever`, `d8a2860105 2026-09-10 fix(auth): report a Cloudflare challenge during discovery as HOST121`, monorepo#1155, code: `"if (!discoveredUrl) {"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:205`, code: `"const fallbackProbe = await tryFallbackDiscovery(normalizedUrl);"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:206`, test “falls back to /wp-json/ when the front-page probe hangs past its timeout” and “discovers the fallback URL when only the Link-header probe is challenged” — platform: all.
3. Discovery messaging: An installed older WCPOS API gets update instructions, not “API not found”; missing/hidden current routes remain a distinct error — evidence: `5b2d3ca44d 2026-08-23 fix(auth): tell merchants to update an outdated plugin instead of "API not found"`, `0e2a9f4173 2026-08-23 fix(auth): prefer a compatible reported plugin version`, code: `"(!!reportedVersion && !isWcposPluginCompatible(reportedVersion)) ||"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:273`, code: `"t('common.please_update_your_woocommerce_pos_plugin'),"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:294`, code: `"ERROR_CODES.WCPOS_PLUGIN_OUTDATED"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:295`, code: `"ERROR_CODES.REST_ROUTE_MISSING"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:319` — platform: all.
4. Extends line 2: Bound each front-page and fallback probe to ten seconds, and distinguish an exhausted timeout from an answered non-WordPress site — a hung front page must not leave Connect spinning forever or be reported as the wrong kind of site — evidence: `c7242dc18b 2026-08-12 fix(auth): bound store-discovery probes so a hung server can't spin Connect forever`, monorepo#1155, code: `"const DISCOVERY_PROBE_TIMEOUT_MS = 10_000;"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:26`, code: `"timedOut ? t('auth.site_took_too_long_to_respond') : t('auth.site_does_not_seem_to_be')"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:223`, test “reports a timeout instead of "not a WordPress site" when both probes hang” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
5. Keep both URL probes quiet on routine failures — asking whether an arbitrary URL is WordPress must not turn a typo into an application-error log or an iOS dev-client redbox — evidence: `333d1a01a5 2026-08-29 fix(e2e,auth): dismiss the iOS dev-menu sheet; stop logging a typo'd store URL as an error (#1663)`, code: `"quietErrors: true,"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:109`, code: `"quietErrors: true,"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:152` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
6. Treat a JSON 401/403 from the fallback as proof the REST API exists, then report restricted access using the server’s message when available rather than misclassifying the site as non-WordPress — evidence: `480571d161 2026-04-14 fix(auth): display connection errors and handle restricted REST APIs`, code: `"if (isWpRestApiError(err)) {"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:166`, code: `"(status === 401 || status === 403) &&"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:142`, code: `"? serverMessage"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:151`, code: `": t('auth.rest_api_restricted');"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:152` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
7. Extends line 2: Recognize cf-mitigated challenge headers case-insensitively and report a bot-challenge code when challenged URL discovery has no successful result or the API fetch is challenged — a mixed-case header must not slip past classification — evidence: `d8a2860105 2026-09-10 fix(auth): report a Cloudflare challenge during discovery as HOST121`, `b52096caf6 2026-09-10 fix(auth): match the cf-mitigated header case-insensitively`, code: `"const key = Object.keys(headers).find((name) => name.toLowerCase() === 'cf-mitigated');"` in `packages/core/src/screens/auth/hooks/bot-challenge.ts:10`, code: `"errorCode: ERROR_CODES.BOT_CHALLENGE_BLOCKING_API,"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:219`, code: `"throw errorWithCode(message, ERROR_CODES.BOT_CHALLENGE_BLOCKING_API);"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:124`, test “recognises a challenge whose header name is not lower-cased” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
8. Normalize entered site URLs to HTTPS for security rather than preserving an insecure protocol — evidence: `eb859bfd0e 2025-07-31 update auth flow`, code: `"const protocol = 'https'; // Always use HTTPS for security"` in `packages/core/src/screens/auth/hooks/use-url-discovery.ts:89` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
9. Prefer the lightweight site endpoint and fall back to the REST index only on 404, not a timeout; bound both fetches to fifteen seconds so a slow but live index can answer without an infinite Connect spinner — evidence: `c7242dc18b 2026-08-12 fix(auth): bound store-discovery probes so a hung server can't spin Connect forever`, `8ed2d42cae 2026-08-19 feat(auth): store discovery prefers wcpos/v2/site over the REST index`, code: ``"response = await http.get(`${baseUrl}wcpos/v2/site`, {"`` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:195`, code: `"timeout: 15_000,"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:201`, code: `"if (get(error, ['response', 'status']) !== 404) {"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:204`, code: `"response = await http.get(wpApiUrl, { params: { wcpos: 1 }, timeout: 15_000 });"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:207`, test “falls back to the API index when the light endpoint returns 404” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
10. Carry classified authorization failures through connect as an error code plus translated inline copy, using dedicated token/transport messages and a generic host message for other codes, and clear both on reset — the documentation link, not untranslated registry prose, names the exact cause — evidence: `14b2455cf8 2026-08-22 feat(core,utils): name the host-blocked auth and transport conditions (Package C, step 1)`, `7b6f9fa4bb 2026-08-22 fix(core): localize the host-blocked connect messages`, code: `"authResult.code === ERROR_CODES.AUTH_TOKEN_BLOCKED_BY_HOST"` in `packages/core/src/screens/auth/hooks/use-site-connect.ts:263`, code: `"errorCode: authResult.code,"` in `packages/core/src/screens/auth/hooks/use-site-connect.ts:269`, code: `"setErrorCode(null);"` in `packages/core/src/screens/auth/hooks/use-site-connect.ts:122`, test “exposes and resets the %s coded connect error” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
11. Block saving a site when compatibility probes report a blocking condition, but continue when they report only warnings — a shared-cache replay must surface its code without turning advisory findings into connection failures — evidence: `64425aa0bc 2026-08-22 feat(core,utils,sync): name the search-WAF, cache-replay and rate-limit conditions (Package C, step 3)`, code: `"if (compatibility.blocking) {"` in `packages/core/src/screens/auth/hooks/use-site-connect.ts:279`, test “exposes a blocking shared-cache replay with the generic translated host message” and “continues connecting when compatibility probes return only warnings” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
12. Log only selected identity fields while processing a successful login response, never the raw response containing access and refresh tokens — evidence: `511a28cf7b 2026-08-20 fix(core): claim redirect-return logins per initiating consumer; harden CSRF and expansion`, code: `"// Never log the raw response - it carries access/refresh tokens."` in `packages/core/src/screens/auth/hooks/use-login-handler.ts:56`, code: `"context: { uuid: params.uuid, id: params.id, display_name: params.display_name },"` in `packages/core/src/screens/auth/hooks/use-login-handler.ts:58` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
13. Extends line 1: Link saved credentials to the site through an atomic append only when not already linked — concurrent login and discovery writes must not lose the locally owned link — evidence: `aff66aa2d8 2026-08-06 fix(auth): atomic wp_credentials link — login no longer races connect's site writes (#902) (#1013)`, issue #902, PR #1013, code: `"// Link credentials to site if not already linked."` in `packages/core/src/screens/auth/hooks/use-login-handler.ts:127`, code: `"const linked = await linkCredentialsToSite(site, credentialsData.uuid);"` in `packages/core/src/screens/auth/hooks/use-login-handler.ts:129` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
14. Extends line 3: Use neutral translated API-not-found toast copy for both hidden routes and a genuinely absent plugin, leaving namespace evidence in structured logs instead of presenting one unproven diagnosis to the merchant — evidence: `63f7a07f70 2026-08-24 fix(auth): stop showing a dev diagnosis as the merchant's toast`, code: `"toast: { title: t('auth.woocommerce_pos_api_not_found') },"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:309`, code: `"namespaces: namespaces.filter((namespace) => namespace.startsWith('wcpos/')),"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:314`, test “shows the merchant the API message, not the dev diagnosis (%s)” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
15. Require the WooCommerce namespace even in a lightweight site response, reporting a coded WooCommerce-missing error rather than allowing discovery to succeed without the store API — evidence: `8ed2d42cae 2026-08-19 feat(auth): store discovery prefers wcpos/v2/site over the REST index`, `5b2d3ca44d 2026-08-23 fix(auth): tell merchants to update an outdated plugin instead of "API not found"`, code: `"if (!namespaces.includes(wcNamespace)) {"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:253`, code: `"throw errorWithCode(t('auth.woocommerce_api_not_found'), ERROR_CODES.WOOCOMMERCE_MISSING);"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:258`, test “reports the existing WooCommerce error for a light response missing wc/v3” — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
16. Mark both lightweight and legacy API discovery requests with the wcpos query parameter for CORS handling — discovery must retain the server’s POS-request signal — evidence: `eb859bfd0e 2025-07-31 update auth flow`, `8ed2d42cae 2026-08-19 feat(auth): store discovery prefers wcpos/v2/site over the REST index`, code: `"wcpos: 1,"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:197`, code: `"response = await http.get(wpApiUrl, { params: { wcpos: 1 }, timeout: 15_000 });"` in `packages/core/src/screens/auth/hooks/use-api-discovery.ts:207` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
