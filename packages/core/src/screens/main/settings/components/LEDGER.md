# Behaviour ledger: `main/settings/components`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/settings/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Give settings pages a shared row, section and destructive-footer layout.

**Composes:** `FormItem`, `FormLabel`, `Label`, `Text`, `Button` and React Native `View`.

## Lines

1. Rows have no dividers; only sections do — evidence: `69a36ee74c 2026-07-22 feat(settings): redesign settings pages with quiet row-based layout`, code: `"No divider — rows separate by rhythm alone."` in `packages/core/src/screens/main/settings/components/settings-row.tsx:24`, code: `"uppercase title — the only rule is a light divider between sections."` in `packages/core/src/screens/main/settings/components/settings-section.tsx:18` — platform: all.
2. `SettingsRow` deliberately degrades to a plain label outside a form provider so the customer-display page matches — evidence: `758b4e71e3 2026-09-04 fix(settings): let SettingsRow render on screens without a form provider`, code: `"const RowLabel = inForm ? FormLabel : PlainLabel;"` in `packages/core/src/screens/main/settings/components/settings-row.tsx:33`, code: `"Same padding as FormLabel so rows look identical with or without a form."` in `packages/core/src/screens/main/settings/components/settings-row.tsx:60` — platform: all.
3. Extends line 1: the first section has no top divider; the rule separates sections rather than framing the page — evidence: code: `"First section on a page has no top divider."` in `packages/core/src/screens/main/settings/components/settings-section.tsx:10` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
4. Keep compact inline controls beside their labels at every width; ordinary rows stack above full-width controls on small screens and put labels left on larger screens — evidence: code: `"Keep label and control on one line at every screen size"` in `packages/core/src/screens/main/settings/components/settings-row.tsx:14`, code: `"other compact controls). Default rows go label-left on md+, stacked on sm."` in `packages/core/src/screens/main/settings/components/settings-row.tsx:15`, code: `"small screens with a full-width control."` in `packages/core/src/screens/main/settings/components/settings-row.tsx:25` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
5. Keep destructive meta-actions in a quiet explained footer outside the form flow, rather than among ordinary settings — evidence: code: `"settings) — out of the form flow, with an explanation of what it does."` in `packages/core/src/screens/main/settings/components/settings-danger-zone.tsx:17` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
