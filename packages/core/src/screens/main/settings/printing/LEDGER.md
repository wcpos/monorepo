# Behaviour ledger: `main/settings/printing`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/settings/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** List printers and receipt templates, route templates and report test-print outcomes.

**Composes:** `PrinterRow`, `TemplateRow`, `PrintersEmptyState`, `SettingsSection`, printer dialogs, buttons, menus and toasts.

## Lines

1. A "sent" toast is never phrased as "printed" unless the lane answers — evidence: `b3cd607570 2026-09-06 feat(printer): P1 — trouble lines from the signatures we already compute; one line for every raw error; edit dialog and row Test Print under the setup rulings (roadmap#161)`, code: `"Only a lane that answers proves the paper moved; everything else was merely sent."` in `packages/core/src/screens/main/settings/printing/index.tsx:165`, code: `"both reply once the printer has taken the bytes. A raw 9100 socket accepts and says"` in `packages/core/src/screens/main/settings/printing/utils.ts:45` — platform: all.
2. The templates chip says "Thermal", not "ESC/POS", because the wire format depends on the printer — evidence: `cdc19f6c57 2026-07-26 fix(settings): label thermal templates 'Thermal', not 'ESC/POS'`, code: `""Thermal" and not "ESC/POS":"` in `packages/core/src/screens/main/settings/printing/utils.ts:22`, code: `"printer (ESC/POS, StarPRNT, ePOS-XML, …)"` in `packages/core/src/screens/main/settings/printing/utils.ts:24` — platform: all.
3. Keep Windows spooler queues visible alongside Print Dialog and label them as installed/system printers rather than exposing opaque queue keys — only the built-in dialog is excluded when deciding whether there are visible targets — evidence: code: ``"A Windows winspool queue is also `connectionType: 'system'`; only the Print Dialog is invisible."`` in `packages/core/src/screens/main/settings/printing/index.tsx:203`, code: `"the queue key, not something a cashier recognises."` in `packages/core/src/screens/main/settings/printing/printer-row.tsx:66` — platform: electron. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
4. Do not offer local edit, delete or default mutations for built-in and server-owned printer targets — synthesized profiles have no mutable local document — evidence: code: `"Built-in/server-owned targets are not backed by mutable printer_profiles documents."` in `packages/core/src/screens/main/settings/printing/printer-row.tsx:82` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
5. Test a cloud printer through the server diagnostic endpoint rather than a local transport — the request must identify the server-owned cloud printer — evidence: test "uses the server diagnostic endpoint when testing a cloud printer" in `packages/core/src/screens/main/settings/printing/index-cloud.test.tsx:261`, code: `"await cloudHttp.post('/print-jobs/test', {"` in `packages/core/src/screens/main/settings/printing/index.tsx:159` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
6. Keep setup and network discovery out of saved printer rows and the Printing list; show the printer guide once outside the rows — a saved printer has already completed setup — evidence: test "renders a saved printer without the wizard entry, even when the wizard is available" in `packages/core/src/screens/main/settings/printing/printer-row.test.tsx:66`, test "does not render network scan controls on the Printing settings screen" in `packages/core/src/screens/main/settings/printing/index-cloud.test.tsx:332`, test "renders help once outside saved rows and opens the printer guide" in `packages/core/src/screens/main/settings/printing/index-cloud.test.tsx:350` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
7. Wait for printer loading to finish before showing the list or empty state — an unresolved fetch must not present a false empty page — evidence: test "hides the list until loaded, then shows the empty state" in `packages/core/src/screens/main/settings/printing/index-cloud.test.tsx:340` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
8. Show one actionable translated line for a test-print failure instead of the raw transport string — evidence: test "shows one actionable line instead of the raw failure string" in `packages/core/src/screens/main/settings/printing/index-cloud.test.tsx:312` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
9. Make a newly inserted built-in Print Dialog the default only when no default printer already exists — initialization must not steal an existing routing choice — evidence: code: `"Only set as default if no other printer is already default"` in `packages/core/src/screens/main/settings/printing/use-ensure-system-printer.ts:24` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
