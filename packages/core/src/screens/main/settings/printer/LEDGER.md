# Behaviour ledger: `main/settings/printer`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/settings/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Configure, test and persist printer profiles across platform-specific connection lanes.

**Composes:** `PrinterDialogLayout`, connection pickers, `AdvancedSettings`, `PrinterDialogFooter`, `TestPrintError` and `PrinterSetupDialog`.

## Lines

1. The printers list waits up to 2 s for cloud printers so the empty state never flashes, and never blanks on a hung request — evidence: `316efac3b5 2026-09-05 fix(settings): loading waits for cloud printers (capped), Electron pickers validate the address, wizard-entry tests`, code: `"const CLOUD_SETTLE_GRACE_MS = 2000;"` in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.ts:21`, code: `"request must not blank the page either, so the wait is capped."` in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.ts:28` — platform: all.
2. `AdvancedSettings` opens by default when editing an existing printer but stays closed when adding — evidence: `f8b891a6ee 2026-05-15 feat(settings): native Add Printer entry — connection-type picker, BT/USB device pickers`, code: `"defaultOpen={isEditing}"` in `packages/core/src/screens/main/settings/printer/add-printer.tsx:167`, code: `"defaultOpen={isEditing}"` in `packages/core/src/screens/main/settings/printer/add-printer.web.tsx:264`, code: `"defaultOpen={isEditing}"` in `packages/core/src/screens/main/settings/printer/add-printer.electron.tsx:310` — platform: all.
3. Preserve the saved language and port when opening a printer, then derive defaults for genuine vendor changes using the vendor the form actually holds — a stale watch must neither overwrite StarPRNT nor leave later changes waiting forever on a reset latch — evidence: `6e9dd2561b 2026-09-12 fix(printer): Star receipts centre scaled text and print the time correctly`, `c013c764b4 2026-09-12 fix(printer): compare the vendor the form holds, not the watched value`, code: `"const currentVendor = form.getValues('vendor');"` in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.ts:228`, test "keeps a saved language the vendor default would overwrite" in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.test.tsx:88`, test "still derives after an external vendor correction lands before the watch publishes" in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.test.tsx:127` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-12 after the source ledger's read)_
4. Subscribe form watches before the open/prefill reset effects — native passive-effect ordering otherwise misses the reset and leaves mount-time values visible until the next edit — evidence: code: `"The watches are declared BEFORE every effect below on purpose."` in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.ts:119` — platform: native. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
5. Only a cashier-selected or probe-confirmed vendor may replace an unset or previous-default port with its own default — a name-based guess must not overwrite a configured port — evidence: code: `"the vendor's default port may only overwrite a configured one when the vendor is known"` in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.ts:109`, code: `"(currentPort == null || currentPort === previousDefaults.port)"` in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.ts:238` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
6. Claim vendor detection only when a probe answered on a real lane and clear the detection on failure — a guessed name or switched-off printer must not earn a detected label — evidence: code: `"a vendor guessed from the name is not a detection."` in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.ts:271`, code: `"A probe that never answered leaves nothing detected:"` in `packages/core/src/screens/main/settings/printer/dialog/use-printer-dialog-form.ts:297` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
7. Allow generic native vendors on network and plain ESC/POS BLE/SPP lanes, but constrain SDK Bluetooth/USB to Epson or Star consistently in schema and dialogs — a saved profile must reopen without its vendor being rewritten — evidence: code: `"one rule so a profile that saves can also be reopened without its vendor being rewritten."` in `packages/core/src/screens/main/settings/printer/schema.ts:89` — platform: native. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
8. Resolve raw TCP 9100 or a missing port to the vendor’s browser protocol port before filling the form — the displayed port must match the endpoint printing actually uses — evidence: code: `"Raw TCP 9100 (and a missing port) resolve to the vendor's web-protocol"` in `packages/core/src/screens/main/settings/printer/web-network-defaults.ts:25` — platform: web. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
9. Respect explicitly selected HTTP or HTTPS printer ports even when the page uses the other scheme — the endpoint preview must agree with the transport — evidence: test "uses https for an explicit Star HTTPS port even on an http origin" in `packages/core/src/screens/main/settings/printer/add-printer-web-defaults.test.ts:34`, test "keeps http for an explicit Star port 80 even on an https origin" in `packages/core/src/screens/main/settings/printer/add-printer-web-defaults.test.ts:42`, test "keeps http for an explicit Epson port 8008 even on an https origin" in `packages/core/src/screens/main/settings/printer/add-printer-web-defaults.test.ts:48` — platform: web. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
10. List OS-paired Classic printers on Electron’s Bluetooth tab, using Windows spooler queues or macOS/Linux serial keys while preserving the selected connection type — paired printers must be where the cashier looks, and routing follows the device key — evidence: code: ``"BLE chooser but surface as installed spooler queues (`winspool:*`). Listed on the"`` in `packages/core/src/screens/main/settings/printer/dialog/connection/os-printers-section.tsx:19`, code: ``"keeps the form's current connectionType; printing routes natively by the `winspool:`"`` in `packages/core/src/screens/main/settings/printer/dialog/connection/os-printers-section.tsx:21`, code: `"device keys. Keep them surfaced in the Bluetooth picker even if the display"` in `packages/core/src/screens/main/settings/printer/dialog/connection/discovered-printer-filters.ts:16` — platform: electron. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
11. Load USB candidates when the tab mounts and keep Refresh for hot-plugging; include Windows installed queues as USB candidates because that is its printable USB-class path — evidence: code: `"mount — the button only re-checks after hot-plugging, since there is no USB"` in `packages/core/src/screens/main/settings/printer/dialog/connection/usb-printers-section.tsx:12`, code: `"only printable Windows path for USB-class receipt printers."` in `packages/core/src/screens/main/settings/printer/dialog/connection/discovered-printer-filters.ts:25` — platform: electron. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
12. Rank printer-like Bluetooth names first and hide unnamed devices behind a toggle; bound the chooser’s scrolling height so surrounding phones and beacons cannot push the dialog actions out of reach — evidence: code: `"lead, other named devices follow, unnamed devices hide behind a toggle: a shop"` in `packages/core/src/screens/main/settings/printer/dialog/connection/electron-bt-picker.tsx:24`, code: `"dialog's own buttons stay in reach."` in `packages/core/src/screens/main/settings/printer/dialog/connection/electron-bt-picker.tsx:58` — platform: electron. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
13. Store character capacity rather than physical paper width and offer both 42 and 48 columns for 80 mm printers — one paper size does not imply one capacity — evidence: code: `"This field stores printer character capacity (CPL), not physical paper width."` in `packages/core/src/screens/main/settings/printer/components/paper-width-select.tsx:13`, code: `"80mm printers are commonly configured as either 42 or 48 columns, so expose both."` in `packages/core/src/screens/main/settings/printer/components/paper-width-select.tsx:14` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
14. Name receipt code pages for their language and offer only tables the encoder knows, using Thai table 11 rather than unsupported cp874 — unknown names silently fall back to Automatic — evidence: code: `"name it does not know falls back to 'auto' at encode time, so the list must not outrun it."` in `packages/core/src/screens/main/settings/printer/components/code-page-select.tsx:11`, code: `"(cp874 is not in it, so offering cp874 would just fall back to 'auto')."` in `packages/core/src/screens/main/settings/printer/components/code-page-select.tsx:24` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
15. Leave Automatic code pages unwritten — missing code-page fields already mean the encoder’s per-string choice, so existing profiles stay untouched — evidence: code: `"already gets — so it stays unwritten and existing profiles are untouched."` in `packages/core/src/screens/main/settings/printer/profile-config.ts:100` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
16. Update the existing printer row when adding an address already configured — one physical address must not create a twin profile — evidence: code: `"One printer = one row: adding an address that is already set up updates that row."` in `packages/core/src/screens/main/settings/printer/persist-printer-profile.ts:14` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
17. Write the target printer successfully before demoting other defaults — a failed save must not first remove the working default — evidence: code: `"Demote the other defaults only once the target row is safely written."` in `packages/core/src/screens/main/settings/printer/persist-printer-profile.ts:27` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
18. Map specific transport failures before generic permission or timeout errors to one actionable line, retaining the raw message in support details rather than the main error copy — evidence: code: `"failures sit above the generic permission/timeout catches."` in `packages/core/src/screens/main/settings/printer/describe-printer-error.ts:12`, code: `"transport string stays in the support details, never on screen."` in `packages/core/src/screens/main/settings/printer/dialog/test-print-error.tsx:51` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
19. Keep test/save failures inside the form body rather than the footer, with copyable details behind a disclosure; omit the printer-guide link when the setup screen already offers it — evidence: code: `"TestPrintError, not here."` in `packages/core/src/screens/main/settings/printer/dialog/printer-dialog-footer.tsx:24`, code: `"The setup dialog already offers the printer guide; a second link on the same screen is noise."` in `packages/core/src/screens/main/settings/printer/dialog/test-print-error.tsx:44` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
20. Build support reports from printer facts, probes, setup outcome and only the last 50 printer log entries in oldest-first order — reports leave the device only when the merchant chooses to copy or share them — evidence: code: `"lines that led there. The merchant copies it; nothing leaves the device by itself."` in `packages/core/src/screens/main/settings/printer/setup-report.ts:42`, test "keeps only printer log lines, the last 50, oldest first" in `packages/core/src/screens/main/settings/printer/setup-report.test.ts:6` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
21. Copy setup reports on web/Electron and use the share sheet on native — native has no clipboard API here — evidence: code: `"Web/Electron copy to the clipboard; native opens the share sheet (no clipboard API)."` in `packages/core/src/screens/main/settings/printer/copy-setup-report.tsx:18` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
22. Merge local printers with a single normalized built-in Print Dialog and synthesized read-only cloud printers, accepting singleton or keyed server payloads rather than dropping those printers — evidence: test "returns local profiles, a built-in system target, and synthesized read-only cloud profiles" in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.test.ts:27`, test "normalizes the legacy built-in system target into the current system target" in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.test.ts:71`, test "normalizes keyed cloud printer payload objects returned by persisted server settings" in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.test.ts:103`, test "normalizes a single cloud printer payload object to an array" in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.test.ts:92` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
23. Extends line 1: wait for the first local printer emission as well as cloud settlement — initial loading is not an empty local list — evidence: test "waits for the first local emission: %j" in `packages/core/src/screens/main/settings/printer/use-available-printer-profiles.test.ts:134` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
24. Preserve discovered or edited connection types rather than forcing network, with current form selection taking priority over prefill — evidence: test "preserves discovered bluetooth connection types instead of forcing network" in `packages/core/src/screens/main/settings/printer/profile-config.test.ts:4`, test "preserves an existing usb printer connection type while editing" in `packages/core/src/screens/main/settings/printer/profile-config.test.ts:35`, test "takes connectionType from form data over prefill" in `packages/core/src/screens/main/settings/printer/profile-config.test.ts:145` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
25. Carry cloud printer identity/provider only on cloud profiles and drop stale cloud metadata when changing to a local connection — evidence: test "omits a stale cloudPrinterId when changing a cloud profile to non-cloud" in `packages/core/src/screens/main/settings/printer/profile-config.test.ts:205`, test "persists cloudProvider on a cloud profile" in `packages/core/src/screens/main/settings/printer/profile-config.test.ts:256`, test "omits cloudProvider from non-cloud profiles" in `packages/core/src/screens/main/settings/printer/profile-config.test.ts:286` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
26. Warn when Star hardware uses ESC/POS or non-Star hardware uses a Star language, but stay quiet when vendor and emulation agree — evidence: test "renders a warning when Star vendor uses ESC/POS language" in `packages/core/src/screens/main/settings/printer/dialog/drawer-emulation-warning.test.tsx:25`, test "renders a warning when a non-Star vendor uses Star language" in `packages/core/src/screens/main/settings/printer/dialog/drawer-emulation-warning.test.tsx:31`, test "renders nothing when vendor and language match" in `packages/core/src/screens/main/settings/printer/dialog/drawer-emulation-warning.test.tsx:37` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
27. Show only platform-supported connection types and emit stable connection-type values; a selected web vendor has both visible and accessible state, with no false selected option for unsupported values — evidence: test "renders only the platform-supported connection types passed by the dialog" in `packages/core/src/screens/main/settings/printer/dialog/connection/connection-type-segmented.test.tsx:66`, test "emits stable connection-type values when selected" in `packages/core/src/screens/main/settings/printer/dialog/connection/connection-type-segmented.test.tsx:82`, test "marks the selected vendor with an accessible and visible active state" in `packages/core/src/screens/main/settings/printer/dialog/connection/web-vendor-segmented.test.tsx:60`, test "does not show a supported web vendor as selected for unsupported values" in `packages/core/src/screens/main/settings/printer/dialog/connection/web-vendor-segmented.test.tsx:89` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
28. Apply a discovered model’s character width and clear stale port state when selecting a target without a port — selection must not inherit the previous printer’s endpoint — evidence: test "applies an identified model width when selecting a scan result" in `packages/core/src/screens/main/settings/printer/dialog/connection/network-fields.test.tsx:159`, test "clears stale port state when the selected target has no port" in `packages/core/src/screens/main/settings/printer/dialog/connection/network-fields.test.tsx:287` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
