# Behaviour ledger: `main/settings/printer/setup`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/settings/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Guide printer discovery, a cashier-requested test page and confirmation before saving.

**Composes:** `usePrinterSetupFlow`, `PrinterService`, printer discovery, dialog/form controls, vendor and code-page selectors, `TestPrintError` and `CopySetupReport`.

## Lines

1. Wizard secondary actions are buttons, not links — "Paul, 2026-09-05: buttons over links" — evidence: `a42a1e4fdc 2026-09-05 style(printer): secondary actions back to buttons, wide modal back (Paul's call)`, code: `"Secondary actions are compact buttons in a wrapping row (Paul, 2026-09-05: buttons over links)."` in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.tsx:220` — platform: all.
2. Preselect a sole printable result but never auto-print; chooser picks and checked addresses become selected cards, with every test page still requiring the cashier’s tap — evidence: code: `"Printing is the cashier's tap (Paul, 2026-09-05): a sole find is pre-selected, never auto-printed."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:424`, code: `"The chooser's pick lands as a selected card; the test page is still the user's tap."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:461`, code: `"The checked address lands as a selected card; printing stays the cashier's tap."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:524` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
3. List early plugged-in and OS-paired results while Wi-Fi scanning continues; a completed scan may refresh the list but must not interrupt an already-selected printer’s test flow — evidence: code: `"Plugged-in and OS-paired printers enumerate in a second; list them while the Wi-Fi scan continues."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:431`, code: `"A printer tapped while the Wi-Fi scan was still running is already printing; only refresh the list."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:421` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
4. Stop scanning when the cashier enters a known address or presses Stop, and ignore any scan that completes afterward — stale discovery must not replace the manual results — evidence: code: `"A scan that finishes after Stop or a manual check must not touch the list."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:175`, code: `"The cashier already knows the address: stop looking and keep whatever was found so far."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:470` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
5. Opening owns a fresh setup session; closing or unmounting stops discovery, cancels pending Bluetooth scanning and disposes the printer service — evidence: code: `"Opening mounts this setup session; closing must stop discovery and dispose its service."` in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.tsx:176`, code: `"The hook owns the scan lifecycle and must stop discovery on unmount."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:545` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
6. Open browser and Electron device choosers only from a tap; native SDK discovery enumerates Bluetooth/USB directly and must not show redundant picker buttons — evidence: code: `"The browser pickers and the Electron chooser only open from a tap:"` in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.tsx:277`, test "scans with the SDKs on native, without picker buttons" in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.test.tsx:268` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
7. Keep a web chooser pending across unrelated discovery updates until its new USB/Bluetooth device arrives, then select that device rather than consuming the request on a network sweep — evidence: test "adopts a new web USB row after intervening discovery updates as the selected card" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:311` — platform: web. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
8. Route native Epson Secure Printing through the folded encrypted target when available, retaining the printer address if no target was folded — the ordinary network lane cannot print while Secure Printing is on — evidence: code: `"that encrypted target is the only one that prints while the setting is on (Spec L)."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:64`, test "keeps the printer address when Secure Printing has no folded target" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:503` — platform: native. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
9. Do not install an identified lane’s port when the current platform cannot print that protocol — WebPRNT discovered on Electron must not become its raw-socket destination — evidence: code: `"A lane this platform cannot print (e.g. WebPRNT on Electron) must not become the raw port."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:260` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
10. Use plain ESC/POS for identity-less Electron chooser devices and native generic BLE/SPP printers, rather than pretending they are SDK-managed Epson or Star devices — evidence: code: `"A chooser-picked device on Electron carries no identity; plain ESC/POS is the safe profile."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:246`, code: `"lane and an `spp:` row the paired Bluetooth Classic lane, both plain ESC/POS (schema rule)."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:250` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
11. Prefill unknown paper width with 48 columns for Epson/Star and 32 for generic chooser Bluetooth/SPP printers, while leaving width confirmation available — likely paper is a starting point, not a measured result — evidence: code: `"Unknown width: pre-select the likely paper so the toggle starts on it."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:286`, test "keeps a paired Bluetooth Classic printer on plain ESC/POS at 58 mm" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:465` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
12. Query native paper width or an unidentified Electron USB model without blocking the test page, stop waiting after four seconds and ignore results superseded by a later printer or manual width choice — evidence: code: `"const WIDTH_QUERY_TIMEOUT_MS = 4000;"` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:32`, code: `"The width query is a nicety: it never blocks the test page, and a later pick supersedes it."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:327`, code: `"The cashier's pick outranks a width query still in flight."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:479`, test "asks an Electron USB printer its model when the product string is generic" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:546` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
13. Treat a short ruler as a request to choose width and print the next page only after that choice — a short page must not silently save the wrong character capacity — evidence: code: `"The ruler answer opens the width choice; the next page prints on the cashier's pick."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:487`, test "cycles a short ruler from 42 to 48 and prints a second page" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:190` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
14. Require a valid form only for the confirmation that saves; short-page and nothing-printed answers remain available even with invalid form fields because they describe the paper — evidence: code: `"Only saving needs a valid form; 'short' and 'none' are answers about the paper, not the form."` in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.tsx:286` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
15. Go directly to trouble when the printer reports no paper or an open cover, and let that status outrank inferred transport failures — do not ask the cashier to read a page that never came — evidence: code: `"The printer answering "no paper" or "cover open" outranks anything inferred from a failure."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:92`, code: `"The printer said what is wrong: don't ask the cashier to read a page that never came."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:375` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
16. Diagnose trouble from already-collected identity/status signatures: Secure Printing and held queues precede generic failures, and pairing advice applies only to Bluetooth printers — evidence: test "reads Secure Printing off the identity before anything else" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:360`, test "reads a held queue off a 503 on any probed port" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:366`, test "only blames pairing on a Bluetooth printer" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:397` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
17. Send an unpaired Android Classic printer to the phone’s Bluetooth settings — pairing is fixed on the phone, not at the printer — evidence: code: `"The Android SPP lane's own line: the fix is in the phone's Bluetooth settings, not at the printer."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:80`, code: `"Android's own Bluetooth settings page, where a Classic printer gets paired (Android only)."` in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.tsx:65` — platform: android. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
18. Keep a failed manual-address check on the results screen with its editable form, distinct from the save-error screen that retries persistence; retain vendor/name hints so a failed Epson ePOS lane is not probed as anonymous raw TCP — evidence: code: `"Not the save-error screen: that one retries a save. The form stays up with the failure."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:535`, code: `"Name and vendor hints keep identify off raw 9100 on an Epson whose ePOS lane is down."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:510` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
19. Keep raw failure text in the local log while emitting only allowlisted setup-outcome fields — support can diagnose locally without sending arbitrary error text through telemetry — evidence: code: `"The sink allowlists fields; the failure text stays in the local log only."` in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.ts:223` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
20. Offer receipt-language/code-page selection under More options for ESC/POS and hide it for Star — the override belongs to the ESC/POS encoder’s character tables — evidence: test "offers the receipt language under More options for ESC/POS and hides it for Star" in `packages/core/src/screens/main/settings/printer/setup/printer-setup-dialog.test.tsx:286` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
21. List identified office printers without testing them — discovery alone must not make a non-receipt printer a test target — evidence: test "lists an office printer without testing it" in `packages/core/src/screens/main/settings/printer/setup/use-printer-setup-flow.test.ts:210` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
