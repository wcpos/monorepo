# Behaviour ledger: `main/settings/barcode-scanning`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/settings/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Configure scanner input sources and feedback, and diagnose captured scan attempts.

**Composes:** `InputSources`, `ScannerDeviceChooser`, `BarcodeSettings`, `TestPanel`, `SettingsSection`, `SettingsRow`, form controls and collapsible disclosures.

## Lines

1. Scanner list rows use `ghost-quiet` Remove, not destructive red: "a destructive-red button on every row is alarm for a list, not emphasis" — evidence: `dfdcbd8e0c 2026-08-24 refactor(scanner)!: one canonical device key, and a scanner section that stays quiet (#1531)`, code: `"variant="ghost-quiet""` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:125` — platform: all.
2. "Not in range" is muted, never destructive; only a live link earns colour — evidence: `dfdcbd8e0c 2026-08-24 refactor(scanner)!: one canonical device key, and a scanner section that stays quiet (#1531)`, code: `"const tone = status === 'connected' ? 'text-success' : 'text-muted-foreground';"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:102` — platform: all.
3. Status wording is transport-specific on purpose — "not in range" for radio, "not plugged in" for cable — evidence: `dfdcbd8e0c 2026-08-24 refactor(scanner)!: one canonical device key, and a scanner section that stays quiet (#1531)`, code: `"return 'not_plugged_in';"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:55`, code: `"return 'not_in_range';"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:58` — platform: all.
4. The direct-connection apparatus is hidden behind a disclosure so the section does not read as required setup; saved scanners stay outside it — evidence: `dfdcbd8e0c 2026-08-24 refactor(scanner)!: one canonical device key, and a scanner section that stays quiet (#1531)`, code: `"disclosure rather than behind two filled buttons that read as required steps."` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:142`, code: `"Saved scanners stay OUTSIDE that disclosure"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:143` — platform: all.
5. Mid-flow replaces the list: "one thing is happening, so one thing shows" — evidence: `dfdcbd8e0c 2026-08-24 refactor(scanner)!: one canonical device key, and a scanner section that stays quiet (#1531)`, code: `"Mid-flow replaces the list: one thing is happening, so one thing shows."` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:270` — platform: all.
6. Manual UUID entry is an escape hatch, hidden by default because surfacing it made the section read as a form — evidence: `dfdcbd8e0c 2026-08-24 refactor(scanner)!: one canonical device key, and a scanner section that stays quiet (#1531)`, code: `"Manual UUID entry is the "my scanner isn't listed" escape hatch"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:384`, code: `"<Collapsible testID="bluetooth-service-class-id-control">"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:388` — platform: all.
7. Sound previews play through the same platform module at the selected volume — "what you hear is what a scan sounds like" — evidence: `203500b211 2026-08-18 feat(core,database,scanner): scan-sound themes, quieter too-short warning, wizard links`, code: `"playScanSuccess({ theme, volume: form.getValues('barcode_scanning_sound_volume') });"` in `packages/core/src/screens/main/settings/barcode-scanning/settings.tsx:129`, code: `"playScanFailure({"` in `packages/core/src/screens/main/settings/barcode-scanning/settings.tsx:135` — platform: all.
8. Extends line 3: keyboard-mode profiles say registered, never connected — recognition happens per keystroke, with no live link to claim — evidence: code: `"Saying "Connected" there would be a claim we cannot back;"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:33` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
9. Extends line 3: HID-POS uses neutral disconnected wording because WebHID reports no bus, so neither cable nor radio can be asserted — evidence: code: `"hid-pos: WebHID reports no bus, so neither cable nor radio wording is"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:60` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
10. Report a live link only for the saved profile actually open — a registered but unreachable scanner must not look connected — evidence: test "reports a live link only for the profile that is actually open" in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.test.tsx:272` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
11. Normalize and validate manual Bluetooth service UUIDs, reject already-registered keys and upsert by the canonical device key — double Add presses must address one row — evidence: test "rejects a non-canonical UUID without writing a profile" in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.test.tsx:414`, test "does not re-add a service UUID that is already registered" in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.test.tsx:453`, code: `"flight is now idempotent rather than a duplicate row"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:227` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
12. Leave hand-entered UUID profiles unnamed and derive their transport label at render time — persisting a translated device name would freeze the current UI language — evidence: code: `"the translated label here would freeze today's UI language into"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:234` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
13. Describe Android keyboard-mode discovery as registration, not connection — it records device identity rather than opening a transport — evidence: code: `"Android registers a keyboard-mode scanner by device identity; it never"` in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.tsx:176` — platform: android. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
14. Always answer an Electron serial/HID chooser, cancelling the old channel on a transport switch and the pending request on unmount — the main process otherwise stays blocked — evidence: code: `"reply — including when the user switches to the other transport or navigates"` in `packages/core/src/screens/main/settings/barcode-scanning/scanner-device-chooser.electron.tsx:20`, test "cancels a pending chooser on unmount so the main process is not left blocked" in `packages/core/src/screens/main/settings/barcode-scanning/scanner-device-chooser.electron.test.tsx:117` — platform: electron. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
15. Explain the keyboard-mode limitation and offer the setup guide in an empty Electron chooser — an empty device list must not just read as broken — evidence: code: `"The empty chooser is the moment a merchant concludes "broken""` in `packages/core/src/screens/main/settings/barcode-scanning/scanner-device-chooser.electron.tsx:132`, test "teaches the keyboard-mode wall in the empty state, with a wizard link" in `packages/core/src/screens/main/settings/barcode-scanning/scanner-device-chooser.electron.test.tsx:86` — platform: electron. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
16. Feed native capture-input keys to both the trace recorder and production detector — web’s global hotkeys are inert on native, otherwise the detected-barcode readout stays empty — evidence: code: `"must feed BOTH the trace capture and the production detector"` in `packages/core/src/screens/main/settings/barcode-scanning/test-panel.tsx:96` — platform: native. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
17. Do not display Infinity milliseconds for a single or modifier-only keypress — there are no gaps to measure — evidence: code: `"a number a cashier should ever read."` in `packages/core/src/screens/main/settings/barcode-scanning/test-panel.tsx:225` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
18. Split scan attempts after gaps over 1000 ms, finalize 400 ms after the last key and retain the newest five attempts — separate bursts must be analyzable independently — evidence: code: `"const ATTEMPT_GAP_MS = 1000;"` in `packages/core/src/screens/main/settings/barcode-scanning/use-scan-trace-capture.ts:8`, code: `"const SETTLE_MS = 400;"` in `packages/core/src/screens/main/settings/barcode-scanning/use-scan-trace-capture.ts:10`, test "caps history at five attempts, newest first" in `packages/core/src/screens/main/settings/barcode-scanning/use-scan-trace-capture.test.ts:58` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
19. Extends line 4: lead with no setup needed only while no scanners are registered, and explain the operating-system keyboard-mode limitation inside the disclosure — evidence: test "drops the "no setup needed" lead once a scanner is registered" in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.test.tsx:239`, test "explains the operating-system keyboard-mode wall inside the disclosure" in `packages/core/src/screens/main/settings/barcode-scanning/input-sources.test.tsx:245` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
