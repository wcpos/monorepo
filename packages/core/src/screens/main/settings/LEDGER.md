# Behaviour ledger: `main/settings`

Seeded 2026-09-18 from the screen inventory `docs/prototypes/2026-09-12-language/settings/INVENTORY.md` §5 in wcpos/roadmap (PR #281) by wcpos/roadmap#350. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3.

**Job:** Compose the general, tax and appearance settings pages with immediate persistence.

**Composes:** `SettingsPage`, `SettingsSection`, `SettingsRow`, `SettingsDangerZone`, form controls, customer selection, theme tiles, Suspense and ErrorBoundary.

## Lines

1. Settings never has a Save button; `useFormChangeHandler` writes on every change — evidence: `69a36ee74c 2026-07-22 feat(settings): redesign settings pages with quiet row-based layout`, code: `"useFormChangeHandler({"` in `packages/core/src/screens/main/settings/general.tsx:143`, code: `"useFormChangeHandler({"` in `packages/core/src/screens/main/settings/tax.tsx:112`, code: `"useFormChangeHandler({"` in `packages/core/src/screens/main/settings/barcode-scanning/settings.tsx:100` — platform: all.
2. Theme tiles are height-locked to the tallest possible card so all six match across rows, locales and widths; `ThemeGrid` is isolated so `useUniwind()` does not re-render the page and cancel the theme transition — evidence: `7f54b9ba01 2026-08-21 fix(settings): cap theme tile labels and raise the height floor past the true max`, `1eabc2c781 2026-05-02 fix(theming): reduce Uniwind theme transition cancellations on native`, code: `"bg-card min-h-40 flex-1"` in `packages/core/src/screens/main/settings/theme.tsx:48`, code: `"parent modal) to re-render during a theme change — which would cancel"` in `packages/core/src/screens/main/settings/theme.tsx:73` — platform: all.
3. The shipping-tax-class row falls back to the raw slug rather than reading blank while the class list loads — evidence: `dd3810abe3 2026-09-11 feat(settings): lock the four ignored tax inputs to the store (roadmap#256) (#1970)`, code: `"this row is read-only now, so it must never read blank."` in `packages/core/src/screens/main/settings/tax.tsx:63`, code: `"shippingTaxClassSlug);"` in `packages/core/src/screens/main/settings/tax.tsx:69` — platform: all.
4. Build the default-customer resource above its own Suspense boundary and read it below — rebuilding it inside an aborted render causes endless retries — evidence: code: `"render and rebuilt by every Suspense retry, which never ends (#1707)"` in `packages/core/src/screens/main/settings/general.tsx:73` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
5. Unwrap a configured default customer’s engine-record payload before formatting its name, while leaving the plain guest data intact — the formatter expects the wire shape — evidence: code: `"'getLatest' in defaultCustomer ? defaultCustomer.getLatest().payload : defaultCustomer;"` in `packages/core/src/screens/main/settings/general.tsx:116` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
6. Extends line 1: bind general, tax and barcode forms to reactive `values` so external store changes reach the controls without a first-load double reset — evidence: code: `"Also fixes the double-reset issue on first load."` in `packages/core/src/screens/main/settings/general.tsx:126`, code: `"This makes the form reactive to external data changes"` in `packages/core/src/screens/main/settings/tax.tsx:90`, code: `"This makes the form reactive to external data changes"` in `packages/core/src/screens/main/settings/barcode-scanning/settings.tsx:80` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
7. Show the four store-owned tax values read-only, persist only the five editable settings and omit Restore server settings — form changes must not write locked keys — evidence: test "excludes locked keys from a form change while preserving editable changes" in `packages/core/src/screens/main/settings/tax.test.tsx:127`, test "renders the four locked store values" in `packages/core/src/screens/main/settings/tax.test.tsx:147`, test "does not render Restore server settings" in `packages/core/src/screens/main/settings/tax.test.tsx:174` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
8. Link to the site’s WooCommerce tax settings only when the site has a URL — an optional URL must produce no link, not a crash — evidence: code: `"a site without one gets no link, not a crash."` in `packages/core/src/screens/main/settings/tax.tsx:71` — platform: all. _(added 2026-09-18 beyond the inventory's §5 by wcpos/roadmap#350: evidenced by a code comment or test on next)_
