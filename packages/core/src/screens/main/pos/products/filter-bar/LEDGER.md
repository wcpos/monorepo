# Behaviour ledger: `pos/products/filter-bar`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Render and edit merchant-ordered built-in pills and reusable multi-condition filters.

**Composes:** `@wcpos/components/{alert-dialog,button,dnd,error-boundary,hstack,icon,icon-button,input,modal,select,suspense,switch,text,toggle-group,tooltip,tree-combobox,vstack}`; type-only imports from `combobox/types` and `lib/use-hierarchy`.

## Lines

1. Built-in pills and quick filters share persisted ordering; built-ins may be hidden and legacy single-kind filters migrate into conditions — customization must survive the richer filter model — evidence: `840ba67b08 2026-09-04 feat(pos): merchant-ordered filter bar with multi-condition quick filters (model, bar, settings, modal shell)` — platform: all.
2. Normalization drops malformed/invalid entries, keeps the first duplicate ID and appends missing built-ins — damaged or old settings must not remove the available filter controls — evidence: `840ba67b08 2026-09-04 feat(pos): merchant-ordered filter bar with multi-condition quick filters (model, bar, settings, modal shell)`; `a025da3008 2026-09-03 fix(pos): address quick-filter review findings`; test name: `filter-bar-layout.test.ts`, “drops malformed entries, keeps the first id, and appends missing built-in pills” — platform: all.
3. A quick-filter press resets before applying conditions/search/sort; a second active press restores the settings baseline — filters replace rather than accidentally accumulate prior browsing constraints — evidence: `840ba67b08 2026-09-04 feat(pos): merchant-ordered filter bar with multi-condition quick filters (model, bar, settings, modal shell)` — platform: all.
4. Active state requires the complete resulting filters, search and effective sort, with taxonomy IDs compared as sets — extra filters must extinguish activation, and sort-only buttons must be pressable — evidence: `a025da3008 2026-09-03 fix(pos): address quick-filter review findings`; `18f0bbde6d 2026-09-04 fix(pos): carry price/type through the browse-window seeder; sort-aware activation; preview on the POS baseline` — platform: all.
5. The editor permits one row per condition field and a named sort-only filter; incomplete conditions disable Save — avoids duplicate-field overwrites while supporting useful sort presets — evidence: `61e988f8e5 2026-09-04 feat(pos): quick-filter editor pane with live device-match preview`; test names: `quick-filter-editor.test.tsx`, “does not offer a field already used by another row”, “enables Save when a named draft has only a sort field” — platform: all.
6. Zero, cleared and unparsable price input mean no bound; finite negative values remain intact — zero otherwise becomes an invisible bound because the shared numeric input displays it as empty — evidence: `fb2c4fdf4c 2026-09-04 test(e2e): filter bar and quick-filter editor spec; fold a zero price bound into 'no bound'`; `a025da3008 2026-09-03 fix(pos): address quick-filter review findings` — platform: all; motivating empty-input behavior: web.
7. Default sort uses a nonempty sentinel instead of an empty Select item — Radix rejects empty-string item values — evidence: `quick-filter-editor.tsx:65–66`, “The "Default order" option's value. Radix Select (web) rejects an empty-string item value, and no sort field is spelled this way, so it cannot collide with a real field.” — platform: web/Electron.
8. The live preview reports device matches capped at 200+, lists five names and warns without blocking Save at zero — incomplete local residency must not prevent saving a valid preset — evidence: `61e988f8e5 2026-09-04 feat(pos): quick-filter editor pane with live device-match preview` — platform: all.
9. Preview starts from the same publication, stock and default-sort baseline as the POS — otherwise it counts products the saved button cannot show — evidence: `18f0bbde6d 2026-09-04 fix(pos): carry price/type through the browse-window seeder; sort-aware activation; preview on the POS baseline` — platform: all.
10. Preview updates debounce by 250 ms and create the binding above Suspense — typing must not recreate queries on every keystroke, and suspended retries must reuse their resource — evidence: `quick-filter-preview.tsx:67`, “Build the binding above the boundary so a suspended reader retries the same resource (#1707).”; `quick-filter-preview.tsx:73–74`, “Debouncing keeps search and price typing from rebuilding the device query on every keystroke.” — platform: all.
11. Visibility/reordering save immediately, while deletion requires confirmation and closes an editor for the deleted filter — prevents accidental deletion and stale-editor resurrection — evidence: `840ba67b08 2026-09-04 feat(pos): merchant-ordered filter bar with multi-condition quick filters (model, bar, settings, modal shell)`; `a025da3008 2026-09-03 fix(pos): address quick-filter review findings`; test name: `modal.test.tsx`, “closes the editor when the filter being edited is deleted” — platform: all.
12. Price-condition summaries use the store currency formatter — presets must not display a hardcoded dollar currency — evidence: `61e988f8e5 2026-09-04 feat(pos): quick-filter editor pane with live device-match preview` — platform: all.
