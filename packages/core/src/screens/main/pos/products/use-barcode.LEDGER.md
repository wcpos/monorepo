# Behaviour ledger: `pos/products/use-barcode.ts`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Resolve accepted scans into cart additions or explicit lookup outcomes.

**Composes:** None directly.

## Lines

1. Scans remain active throughout the `(pos)` section, including Cart and checkout, but not other sections — routing follows the cashier’s active workflow rather than the Products leaf’s focus — evidence: `985c815a26 2026-08-21 feat(core): POS section owns barcode scans`, fixes #1438 — platform: all.
2. Local misses escalate through the engine’s authenticated transport, force-refresh targeted records, then query locally again — stale resident barcodes must not produce false misses after a server match — evidence: `0217bb8e17 2026-07-14 feat(core+sync-engine): barcode scans escalate online via sync-core resolveScan (increment 6) (#649)` — platform: all.
3. Server ambiguity hydrates at most ten candidates before reconsidering the local matches — avoids presenting an empty choice list or unbounded hydration; publication filtering can reduce ambiguity to one sellable match — evidence: `0217bb8e17 2026-07-14 feat(core+sync-engine): barcode scans escalate online via sync-core resolveScan (increment 6) (#649)`; `af214119ab 2026-07-24 fix(pos): close unpublished product scan gaps` — platform: all.
4. Scan symbology accompanies every local re-query and online resolution — permits identified UPC-E expansion without guessing that an unrelated eight-digit EAN is UPC-E — evidence: `0cbe1a39a1 2026-08-22 fix(barcode): close the UPC-E wedge gap by carrying the scan symbology` — platform: all.
5. Equivalent-code online probes share one ten-second deadline, including an explicit cancellation race — a slow first probe must not grant the next probe another full timeout — evidence: `c3a7081bc4 2026-08-22 fix(barcode): address review — SKU twin, shared deadline, UPC-E ambiguity`; test name: `use-barcode.test.tsx`, “spends ONE ten-second deadline across both probes of an equivalent code pair” — platform: all.
6. Offline status prevents online escalation only after the local lookup misses — resident scans remain usable, and lifecycle/bootstrap gates alone do not prohibit online resolution — evidence: `8bee8e09bd 2026-08-11 feat(pos): move scan-outage feedback from a standing banner to scan-time toasts` — platform: all.
7. Hidden-out-of-stock scan gating uses shared sellability rather than raw `stock_status` — respects optimistic quantities and permits backorders — evidence: `7bba4f2c28 2026-08-20 fix(core): barcode scans gate products on the shared sellability resolver` — platform: all.
8. Variations require their parent, fetching it when absent; non-published parents are rejected and targeted handles are released — a published child must not bypass its parent’s selling visibility — evidence: `af214119ab 2026-07-24 fix(pos): close unpublished product scan gaps`; `0217bb8e17 2026-07-14 feat(core+sync-engine): barcode scans escalate online via sync-core resolveScan (increment 6) (#649)` — platform: all.
9. Variation attributes are sanitized before constructing explicit cart metadata — malformed payloads must not terminate the scan subscription, and explicit metadata avoids duplicated attributes — evidence: `296f98a3fc 2026-08-19 fix(pos): scans resolve the just-switched cart and survive malformed variation attributes`, closes #1294; `3fc64ad137 2024-12-10 fix variation attributes doubling when barcode scanning` — platform: all.
10. Only the newest concurrent scan may change shared search state — a slower old miss must not overwrite a newer scan’s cleared search — evidence: `f94c2fe765 2026-07-18 fix(core): latest-wins guard for scan search state`, review on #729 — platform: all.
11. Success awaits a successful cart mutation, suppresses the add-hook’s duplicate toast and clears pending search drafts; failed online additions replace “Searching” with terminal feedback — prevents false success, double notifications and stranded searching state — evidence: `690165d17b 2026-07-17 fix(core): gate scan success on cart mutation`; `3a043fb375 2026-07-17 fix(core): clear searching-online toast when scan add fails`; `8769a678e3 2026-07-15 feat(core): POS products rides the query-state store + bindings (migration 8/8 — the last screen) (#661)` — platform: all.
12. Storage-worker loss receives storage-specific terminal feedback, including during hydration; ordinary misses/ambiguity log warnings rather than system errors — distinguishes a broken local database from normal scanning outcomes — evidence: `eda3da398f 2026-08-06 fix(pos): degraded-storage signal — barcode scans fail loudly, banner surfaces worker loss (#163) (#1010)`; `bdfaba2a2a 2026-08-14 fix(logs): demote barcode scan misses to warn, widen status column, link help straight to docs` — platform: all.
