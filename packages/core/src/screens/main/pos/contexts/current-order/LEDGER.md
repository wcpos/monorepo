# Behaviour ledger: `pos/contexts/current-order`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Maintain shared current-cart selection, open-order resources and the temporary new-order record.

**Composes:** none directly.

## Lines

1. Separate stable mutation actions from reactive current-order display state and resolve the order when an action executes — product buttons must not subscribe to every cart write merely to hold a click handler — evidence: `context.ts:75–79`, “Every product tile calls `useAddProduct()`,” / “which called `useCurrentOrder()` purely so its click handler could read the order”; `a2cfaf7aca 2026-08-17 fix(pos): stop the products panel re-rendering on every cart write (#1248)` — platform: all.
2. Keep context readers separate from the router/provider import tree and support optional readers outside POS — reading the current order must not drag native router/codegen dependencies into standalone consumers — evidence: `context.ts:21–24`, “into anything that merely wants to READ the current order. That broke the” / “tax-rates provider's test environment”; “Readers import from here; only the provider imports the provider.” — platform: all.
3. Retain one internal selection across products/cart routes; park a stale route ID until an actual route change or explicit reselection, not a new-order save; pass catch-all route parameters as arrays — stale navigation must not resurrect the order the cashier left, and router serialization must not call `.join` on a string — evidence: `1c8575e72e 2026-09-02 fix(pos): the parked route id is released only by the route or an explicit re-selection`; `c55acc5dbf 2026-04-07 fix: pass orderId as array for catch-all route (#296)` — platform: all.
4. Update action-facing current-order refs in a layout effect — scanner input arriving after commit but before passive effects must target the newly selected cart — evidence: `296f98a3fc 2026-08-19 fix(pos): scans resolve the just-switched cart and survive malformed variation attributes`; named timing test — platform: all.
5. Build new/open-order Suspense resources above the boundary that consumes them and bind subscriptions/demand handles to their mount lifetime — pre-commit suspension must not throw away and endlessly rebuild the same resource — evidence: `a2e12d5bb6 2026-08-30 refactor(core): fix the Suspense retry loop by moving boundaries, not by caching`; `6b6040918b 2026-08-30 fix(core): keep every Suspense resource alive across a pre-commit retry` — platform: all.
6. Keep `pos-open`, `pos-partial` and `pending` orders in the open-order resource and preserve unbounded demand with local sorting — recording a partial payment must not remove its order from tabs — evidence: `use-open-orders-resource.ts:24–26`, “record-manual-payment.ts patches status to derive().status, so a part-paid order” / “leaves pos-open and would drop out of the tabs mid-checkout. Include pos-partial” / “and pending to keep the order and its tender reachable, including after reload.” — platform: all.
7. Filter open orders by POS cashier/store metadata, omit store filtering for the no-store sentinel and sort residents by creation time — inferred purpose: tabs should retain the cashier’s relevant carts in stable chronological order — evidence: `use-open-orders-resource.ts`, resident-filter/sort pipeline; _(inferred — no explicit evidence)_ — platform: all.
8. Lazily create the temporary database record through one shared ensure-exists stream, emit by UUID and preserve top-level record fields during payload patches — concurrent subscribers must not double-insert or replace the record envelope — evidence: `4abde85eec 2026-08-21 fix(core): harden temp-order ensure-exists stream and seeding null-path (review)` — platform: all.
9. Fail loudly when a seeded temporary record lacks UUID and log rejected seed metadata writes — an initialization failure must not masquerade as a valid empty cart or silently lose POS identity — evidence: `4abde85eec 2026-08-21 fix(core): harden temp-order ensure-exists stream and seeding null-path (review)`; `e87cbc08b3 2026-08-04 fix(pos): stamp POS identity meta at order save seam and protect it through ack adoption` — platform: all.
