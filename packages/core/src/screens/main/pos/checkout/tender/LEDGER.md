# Behaviour ledger: `pos/checkout/tender`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Compose method selection, keypad, split plans, reader interaction, payment ledger and cancellation into a resumable tender flow.

**Composes:** `@wcpos/components/{button,collapsible,hstack,icon,modal,status-badge,tabs,text,vstack,webview}`.

## Lines

1. Keep the keypad visible, preselect an available method without recording payment and retain a manually edited amount when switching methods; discard invalid remembered choices — choosing a method must not be an extra screen or erase cashier input — evidence: `96eaec238e 2026-09-11 feat(checkout): tender pane slice 1 — the keypad is the screen (roadmap#262)`; `fcb91363df 2026-09-11 fix(checkout): preserve valid tender choices` — platform: all.
2. Enter integer minor units by right-shifting digits, replace the prefill on the first digit and cap the amount at `999999999` minor units — avoid decimal-entry ambiguity and runaway held keys — evidence: `tender-state.ts:31`, “There is deliberately no decimal key: digits shift in from the right.”; `tender-state.ts:65`, “it stops a stuck key running the display off the screen.” — platform: all.
3. Cap the applied cash amount at the current share/balance and treat the remainder as change; forbid excess entry for methods that cannot give change — excess cash is not an overpayment or a larger split leg — evidence: `tender-state.ts:235`, “the excess is change, never an overpayment on the order.”; `90ccd8aa9e 2026-09-09 feat(pos): split payment as a visible plan; this-payment line in both views (#1929)` — platform: all.
4. Offer the exact cash balance plus rounded-up 5/10/50 steps, deduplicated and ascending — quick tender buttons must never suggest less than the amount due — evidence: `tender-state.ts:249–251`, “Quick tendered amounts under a cash keypad: the balance itself, then the next” / “whole 5, 10 and 50 above it. Deduped, ascending, never below the balance.” — platform: all.
5. Support even, fixed-amount, percentage and item splits; round even shares half-up in minor units and leave the final leg as the actual remainder — rounding must not leave an unpayable residual or silently alter the total — evidence: `dcacd3ccd6 2026-09-11 feat(checkout): tender pane slice 2 — split view (roadmap#263)`; `tender-state.ts:270–271`, “Even split shares. Returns the next tender's share, rounded half-up to the” / “minor unit; the last leg remains whatever balance is left.” — platform: all.
6. Derive split progress from qualifying payment rows rather than a reducer counter, and let redivision start after existing paid legs — failed/voided attempts and replanning must not count as paid shares or erase accepted money — evidence: `tender-state.ts:282`, “Completion is derived from payment rows, never an extra counter in the reducer.”; `90ccd8aa9e 2026-09-09 feat(pos): split payment as a visible plan; this-payment line in both views (#1929)` — platform: all.
7. Identify split items by stable POS line UUID before remote ID and include tax when the display is tax-inclusive — synchronization must not unselect an item, and an item share must match the displayed amount — evidence: `ea6d1e1ce6 2026-09-11 fix(checkout): preserve split tender state`; named stable-UUID test — platform: all.
8. Store the active plan and item-paid badges per order across remounts, preserving earlier method badges until a group is fully paid — switching tabs or taking a partial group must not reset or prematurely mark the split — evidence: `ea6d1e1ce6 2026-09-11 fix(checkout): preserve split tender state`; named remount test — platform: all.
9. Freeze the cashier-visible split summary onto the completing leg, including offline cash/device paths, and clear an abandoned stale summary when completing without a plan — order history must describe the split actually used — evidence: `use-tender-flow.ts:507–510`, “The split as the cashier saw it, written with whichever leg completes the sale”; “a” / “split an earlier abandoned attempt left on the order is cleared”; `10f2b3204e 2026-09-14 fix(checkout): the split and the reader survive offline paths, retries and re-attempts` — platform: all.
10. While saving, show disabled real tiles or skeletons and a notice after four seconds; show no tiles after refusal; queued-offline uses offline capabilities and does not enable legacy checkout — saving, refused and durably queued are different cashier states — evidence: `559511b990 2026-09-08 feat(pos): report the three order-save outcomes at the tender pane`; `tender-pane.tsx:28`, “Long enough that a normal save never shows it, short enough that a cashier is not left guessing.” — platform: all.
11. Show only POS-enabled non-webview methods in the normal selector, sort by configured order then title and retain unavailable methods disabled with a reason — extension gaps must be explainable without reshuffling or silently hiding methods — evidence: `tiles.ts:89`, “The order the POS settings page assigns, then title, so the grid never reshuffles itself.”; `tender-pane.tsx:143`, “A method the app cannot drive is shown disabled with the reason, never hidden” — platform: all.
12. Preserve a viable offline queue transport instead of disqualifying a device method solely because its previous transport needs the network — cached reader methods must remain usable on supported offline paths — evidence: `fcb91363df 2026-09-11 fix(checkout): preserve valid tender choices` — platform: all; reader capability dependent.
13. Choose a store-locked default reader first, otherwise a free remembered reader, then a free default, otherwise require an explicit choice; persist confirmed preference per gateway without letting preference I/O fail payment — do not select an arbitrary reader or lose the till’s established choice — evidence: `ddf5414118 2026-09-10 feat(checkout): the till remembers the terminal it last used per gateway (#1950)`; `remembered-readers.ts:52`, “A preference that cannot be read or saved must not interrupt a payment.” — platform: all.
14. Exclude offline server readers, identify readers held by another order and reserve device readers per provider — separate orders must not compete for one terminal collection — evidence: `c204308137 2026-09-08 feat(checkout): server capture-mode tender flow — reader choice, terminal leg view, waiting chip`; `0e663ed79c 2026-09-10 fix(checkout): device harness review pass — bootstrap through the service, provider-neutral offline settlement with bounded retries and single-flight flush, per-driver reservations, reconnect ownership, resume without an order id, public-surface spec` — platform: all.
15. Bootstrap/reconnect through the payment service, cancel stale reconnect ownership and never overwrite a manual reader choice with a late preference read — asynchronous setup must not connect the wrong gateway or reader — evidence: `use-tender-flow.ts:377`, “A local preference can arrive after the tile tap; leave a cashier's choice alone.”; `0e663ed79c 2026-09-10 fix(checkout): device harness review pass — bootstrap through the service, provider-neutral offline settlement with bounded retries and single-flight flush, per-driver reservations, reconnect ownership, resume without an order id, public-surface spec` — platform: all.
16. Recheck connected-reader availability and matching transport immediately before minting a leg, and return stable equal driver-status snapshots — a disconnected reader must not create a phantom pending payment or an extension driver cause an infinite render loop — evidence: `7fb3da4789 2026-09-11 fix(checkout): device flow follow-ups from the live run — receipt stage after capture, decline lands as failed, no leg on a reader that is not connected`; `use-driver-status.ts:41`, “useSyncExternalStore re-renders forever when getSnapshot returns a fresh object each” — platform: all.
17. Let SDK-owned login remain reachable through Connect Reader, use SDK settings where discovery is SDK-owned and expose simulation/offline controls only in development — the login action must not be disabled by its own prerequisite, and test controls must not leak into production — evidence: `tiles.ts:126`, “SDK-owned login is reached through Connect Reader inside the selected tile.”; `99b7a884d5 2026-09-11 feat(checkout): dev-only simulated-card and offline controls for the Stripe Terminal driver's simulated reader` — platform: iOS, Android.
18. Guard Take synchronously against duplicate presses, block mutations while busy/live and check storage before recording — rapid taps must not create multiple payment legs before React rerenders — evidence: `7b38833964 2026-09-03 fix(checkout): close tender review gaps`; `c2660a9776 2026-08-06 feat(pos): degraded storage blocks checkout/save/void with reload CTA (#163 follow-up, ruling R5) (#1019)` — platform: all.
19. Permit partial payment while register choice is outstanding, but block the completing leg and enforce an open session when sessions apply; a store with no remaining registers can still trade — only the completion boundary requires the final register stamp — evidence: `use-tender-flow.ts:493–495`, “Part payments may proceed, the completing leg” / “may not. A store with no register at all keeps trading”; `4311853a17 2026-09-12 feat(pos): the register session in the cart column — local-first sessions and movements, Open register card, Register panel, session on every sale (roadmap#269)` — platform: all.
20. Finish a zero-balance sale with completion metadata but no artificial payment row — inferred purpose: a free/already-settled sale should not invent a money movement — evidence: `use-tender-flow.ts`, zero-total completion branch; _(inferred — no explicit evidence)_ — platform: all.
21. Disable full-order legacy webview checkout once live money exists — a legacy gateway charges the whole order and cannot safely become one leg of a split — evidence: `legacy-tab.tsx:25–26`, “so they can never be one leg of a split, and the tab greys out the moment”; `d9abcb48ff 2026-09-03 feat(checkout): two-pane tender flow — tiles, split, cash to receipt, legacy tab` — platform: all.
22. Use a compact phone selector/keypad and avoid duplicating the ledger balance presentation already supplied by the host — the same tender flow must fit a sheet without repeating its primary amount — evidence: `96eaec238e 2026-09-11 feat(checkout): tender pane slice 1 — the keypad is the screen (roadmap#262)`; `90ccd8aa9e 2026-09-09 feat(pos): split payment as a visible plan; this-payment line in both views (#1929)` — platform: all, responsive by screen size.
23. Keep a live terminal timeline mounted in `TenderCheckout` despite tab/view changes — a live payment must not disappear behind another view — evidence: `0ab00fa481 2026-09-11 fix(checkout): retain terminal timeline across tab switches` — platform: all.
24. Distinguish accepted-but-unmirrored money from a refused row, and fold accepted money into the pane’s next balance even before recovery refresh lands — “not recorded” or the original full balance could invite a second payment — evidence: `use-tender-flow.ts:751–753`, “The store answered 2xx: the money is on the order there, and only this” / “till's copy failed to save. Reporting "payment not recorded" here reads as” / “"take it again"”; `c13bda141e 2026-09-12 fix(checkout): four review findings on the rows that lie (roadmap#294)` — platform: all.
25. Subscribe to terminal outcomes before checkout can unmount; narrate settled failures once per payment even when they arrive as HTTP 200 without an error object — normal asynchronous declines still need a traceable payment row — evidence: `use-tender-flow.ts:854–860`, “the ordinary asynchronous decline arrives as a 200 whose payment row reads” / “`failed`, with no error object at all. The row is always written, once per” / “payment row” — platform: all.
26. Report post-capture completion failure as charged-but-order-unfinished with its own actionable code — saying payment failed after capture can cause a duplicate charge — evidence: `use-tender-flow.ts:908–910`, “The card has been charged by this point: the failure is finishing the” / “order, not taking the money”; `14c050b2b3 2026-09-12 fix(checkout): seven review findings on the diagnosable rows (roadmap#295)` — platform: all.
27. Preserve amount and reader when retrying a finished unsuccessful attempt, but mint a new payment row; retry capture/confirmation on the existing live attempt instead — new collection and confirmation retry must not share the wrong identity — evidence: `10f2b3204e 2026-09-14 fix(checkout): the split and the reader survive offline paths, retries and re-attempts`; test “retry preserves amount and reader but the next Take mints a new row” — platform: all.
28. Explain cash return versus payment void per leg, keep cancellation open on partial failure and distinguish explicit refusal from unknown outcome — the cashier must not manually refund a void whose answer was merely lost — evidence: `use-tender-flow.ts:1024–1026`, “Only a refusal the store actually answered earns the definitive "refund” / “these" instruction. A void whose answer was lost may already have been” / “applied, and refunding it by hand would return the money twice.” — platform: all.
29. Show tendered/change details only for rows that actually contain tendered cash, and label offline authorizations as settling later rather than already server-captured — the running ledger must not misstate how money was accepted — evidence: `144c8d1e7e 2026-09-12 fix(checkout): three tender rows that told the cashier the wrong thing (roadmap#294)`; `311ec56a05 2026-09-10 feat(checkout): app-side card-reader driver harness and the device capture-mode flow` — platform: all.
30. Map terminal phases to a persistent Sent → On terminal → Approved → Captured timeline, retain progress through cancellation/finality and wait for motion preference before animating the ring — feedback must not regress visually or violate reduced motion — evidence: `713549eac7 2026-09-12 feat(checkout): tender pane slice 3 — the terminal moment (roadmap#264)`; tests “keeps Approved through cancellation and release” and “starts the ring only after the motion preference resolves false” — platform: all.
31. Present distinct retry-confirmation, retry-capture, cancel-on-terminal and release actions according to leg/driver state — the interface must not offer a new collection where only verification remains, or imply the app can cancel an SDK-owned interaction — evidence: `311ec56a05 2026-09-10 feat(checkout): app-side card-reader driver harness and the device capture-mode flow`; `713549eac7 2026-09-12 feat(checkout): tender pane slice 3 — the terminal moment (roadmap#264)` — platform: all; native reader actions on iOS/Android.
32. Merge terminal logs chronologically, share them on native and copy them on browser/Electron; omit unavailable clipboard controls and treat export failure as diagnostic rather than payment failure — support output must reflect the displayed sequence without changing the payment outcome — evidence: `terminal-leg-view.tsx:141`, “Match Logs: native uses the share sheet; browser/Electron use the Clipboard API.”; test “merges logs oldest first and copies the displayed lines” — platform: iOS, Android, web, Electron.
33. Translate known payment kinds/statuses/failures, use a neutral Other label for unknown kinds and explain unsupported methods rather than exposing raw contract vocabulary — newer descriptors must remain intelligible to cashiers — evidence: `labels.ts:10–12`, “Rather than render a raw wire value at a till, an” / “unknown one falls back to the neutral "Other" bucket the contract defines” — platform: all.
