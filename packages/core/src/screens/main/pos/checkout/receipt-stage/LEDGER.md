# Behaviour ledger: `pos/checkout/receipt-stage`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Keep the paid sale visible through receipt handling and explicitly finish it into a new sale.

**Composes:** `@wcpos/components/{button,hstack,text}`.

## Lines

1. Resolve the receipt’s selected order independently of the new/current cart and clear a selection whose order disappears — switching stores or purging an order must not strand the receipt host — evidence: `receipt-stage.tsx:114`, “A receipt selection that outlives its order (a store switch, a purge) must not strand the”; `c28f97b32b 2026-09-07 fix(checkout): review round on the column swap` — platform: all.
2. Headline positive change, otherwise the settled paid amount; include captured and recorded-offline authorized rows, not arbitrary pending/failed rows — the Paid moment must describe money actually accepted by this flow — evidence: `39582c6f07 2026-09-11 fix(checkout): correct paid moment settlement details` — platform: all.
3. Show all distinct settled methods in ledger order and sum tendered values only from cash-kind rows, including custom cash methods — mixed splits must not be described as one primary method or mislabel card amounts as cash tendered — evidence: `39582c6f07 2026-09-11 fix(checkout): correct paid moment settlement details`; named mixed-split test — platform: all.
4. Disable both New sale and No receipt while automatic printing is pending — finishing must not destroy a receipt before its automatic print attempt completes — evidence: `c28f97b32b 2026-09-07 fix(checkout): review round on the column swap`; named auto-print test — platform: all.
5. Make receipt actions secondary to New sale; finishing clears receipt/current-order selection and returns the phone to cart — explicit completion, not a receipt action, owns the transition to the next customer — evidence: `f0513ed169 2026-09-07 feat(checkout): taste pass on the column swap from live screenshots`; `f61fdc7f64 2026-09-07 feat(checkout): the receipt is the last stage of the tender pane, not a modal on top` — platform: all.
6. Wait for reduced-motion preference before animating the paid surface/tick and render the final static state when motion is reduced — success feedback must not briefly animate before accessibility preference resolves — evidence: `3756412a7a 2026-09-12 feat(checkout): tender pane slice 4 — the Paid moment (roadmap#265)`; test “renders a complete tick and surface without animation when motion is reduced” — platform: all.
7. Load haptics lazily on native and swallow haptic failure; allow Android Back to finish, but not desktop Escape — optional feedback must not break startup or a recorded sale, and receipt dismissal is deliberate — evidence: `9e48df6259 2026-09-12 fix(checkout): pull expo-haptics lazily — a static import breaks the web connect screen at startup`; `receipt-stage.tsx:73`, “Missing haptic feedback must not interrupt a recorded sale.”; test “finishes on Android hardware back” — platform: web/Electron startup; iOS/Android feedback; Android Back.
8. When capture succeeded but completing or mirroring the order failed after the receipt was selected, renders the captured-but-unfinished notice above the success headline from the retained settlement — the tender error pane is unreachable from the receipt stage, and a success-only receipt would invite a second payment — evidence: `f5501b72b2 2026-09-18 fix(checkout): a captured-but-unfinished sale shows its error on the receipt too (Codex review)`, code: `"<CapturedUnfinishedNotice finishingError={leg.settlement.finishingError} />"` in `packages/core/src/screens/main/pos/checkout/receipt-stage/receipt-stage.tsx:177`. _(added 2026-09-18 after seeding, from the Codex review of monorepo#2160; the behaviour landed after the source ledger's read)_
