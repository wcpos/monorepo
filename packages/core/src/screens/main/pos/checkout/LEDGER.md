# Behaviour ledger: `pos/checkout`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Select and retain each order’s checkout mode, save state, receipt selection and browser URL.

**Composes:** `@wcpos/components/{button,modal,text,toast,vstack,webview}`.

## Lines

1. Checkout belongs to the order, survives tab changes and resumes from live payment rows; an explicitly flagged draft can enter before its first save — optimistic Pay must not wait for a remote ID — evidence: `checkout-mode.ts:224–225`, “A draft has no ledger to derive from, but Pay can flag it before its first” / “save lands; the explicit store entry must swap the columns for it too.” — platform: all.
2. Reset transient checkout state on a store-ID change, not every POS mount — visiting Orders must not discard unfinished checkout — evidence: `checkout-mode.ts:198–199`, “CHANGE of store id only — never on mount, or leaving the POS for the Orders screen and” / “coming back would drop every order that was mid-checkout.” — platform: all.
3. Claim automatic receipt printing once per sale, clearing the claim when that sale ends — remounting a receipt must not print again — evidence: `checkout-mode.ts:35–36`, “Receipt panes remount when cashiers switch tabs; an auto-print attempt belongs” / “to the sale, not the mount. Keep this transient and clear it when the sale ends.” — platform: all.
4. Latch tender-versus-legacy mode when checkout opens — a descriptor refresh must not unmount a live tender flow — evidence: `checkout.tsx:74–77`, “Latched once, when the modal opens.” / “would unmount a tender flow holding live legs without running its cancel guard.” — platform: all.
5. Gate Process Payment on frame readiness and read that readiness through a live ref — fire-and-forget messages otherwise disappear, including presses holding a stale ready closure — evidence: `checkout.tsx:95–98`, “`wcpos-process-payment` is fire-and-forget: no ack, no retry.”; `checkout.tsx:103–104`, “The ref is written the moment the frame re-gates,” / “so the guard reads live state rather than a snapshot.” — platform: all.
6. Block checkout’s money action when local storage is degraded — payment must not advance through an unreliable local write path — evidence: `c2660a9776 2026-08-06 feat(pos): degraded storage blocks checkout/save/void with reload CTA (#163 follow-up, ruling R5) (#1019)` — platform: all.
7. Show a missing-payment-link explanation in the body and disable processing — an absent link must not produce a blank checkout or an actionable dead button — evidence: `1bc178f7bf 2026-07-24 fix(pos): move missing-payment-link message into checkout body; guard links$ for mock orders` — platform: all.
8. Show stock rejection only for the affected order, replacing payment actions with Return to cart — the cashier must resolve rejected stock rather than retry the unchanged sale — evidence: `402c77e68f 2026-07-17 feat(pos): cart stock guard + checkout stock-rejection UX (#724)` — platform: all.
9. Repeat the totals-changed banner inside legacy checkout without blocking payment — the cart’s banner is hidden behind the modal, but server totals remain authoritative — evidence: `checkout.tsx:189–195`, “The cart's copy of this banner is behind this full-height” / “modal”; “It does not gate Process Payment; the server's totals” / “stand, and the cashier decides.” — platform: all.
10. Encode a method ID as one URL segment, give receipt selection precedence and accept only the exact checkout suffix shape — special method IDs and unrelated nested routes must not corrupt checkout routing — evidence: `4869c0f4d8 2026-09-08 feat(pos): mirror checkout stage and method into the URL (#1912)`; `pos-url.ts:24`, “`/cart/<uuid>/checkout[/<method>]` and nothing longer; the router hands us decoded segments.” — platform: web, Electron.
11. Seed checkout once per distinct cold-load URL tuple, waiting for descriptors and routing phones/unsupported stores to their sheet or legacy modal — later tab changes must not re-enter abandoned checkout — evidence: `use-checkout-url-seed.ts:13–17`, “tab switch must not drag the cashier back into a checkout they left.” / “its legacy modal, so the link is routed there instead of into an unusable tender column.” — platform: all.
12. Mirror browser mode with replacement history after router work, omit draft IDs and leave routed modals alone — URL updates must not add navigation steps or overwrite a receipt/modal route — evidence: `4869c0f4d8 2026-09-08 feat(pos): mirror checkout stage and method into the URL (#1912)`; `use-pos-url-mirror.ts:18`, “it owns the address bar, and a cold load of it must not be rewritten to the cart underneath.” — platform: web, Electron.
13. Immediately present an in-progress save as queued-offline on literal network loss, but retain saving when the website is unavailable — do not wait for the engine’s next connectivity tick or misclassify an unresponsive store — evidence: `use-order-save-state.ts:11–14`, “after the network dropped. Only `'offline'` flips it; `'online-website-unavailable'` stays” / “`saving` because that is exactly the "store is not answering" case.” — platform: all.
14. Restore rejected/conflicted/needs-revision holds from durable queue records, resubscribe after same-engine store switches and clear only previously observed holds that disappear — resets and unrelated saves must not silently release a refused order — evidence: `use-rejected-order-saves.ts:25–26`, “then clears every entry — so this effect must run again after that reset, or the new” / “store's existing dead letters would lose their hold until the next queue change.”; `36ec3077db 2026-09-08 fix(checkout): follow coalesced writes, keep tender offline while queued, render a refusal` — platform: all.
15. Extends line 5: Offer an in-place Retry only when the first payment document stalls, not when a later navigation fails — reloading a later page could restart checkout underneath an already-paid sale — evidence: `8a4960175f 2026-09-03 fix(checkout): retry only the first payment document; boundary-keyed remount; no watchdog without a link`; code: "Stalled = the first document never came; retrying its navigation is safe." in `packages/core/src/screens/main/pos/checkout/checkout.tsx:129` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: present at the read, missed by it)_
16. Enter a background-paid order into receipt state without selecting it, retaining its paid tab for later — another order finishing must not replace the sale the cashier is serving — evidence: `e81a5fe335 2026-09-08 fix(checkout): background capture never switches the screen; cancel waits for an in-flight intent`; test "a background capture enters the receipt without selecting it"; code: "selectedReceiptOrder: select ? uuid : snapshot.selectedReceiptOrder," in `packages/core/src/screens/main/pos/checkout/checkout-mode.ts:145` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: present at the read, missed by it)_
17. Extends line 14: Queue only an active save or the same queued mutation, and clear a queued hold only for its matching mutation — a stale acknowledgement must neither clear a newer save nor release a rejection — evidence: `559511b990 2026-09-08 feat(pos): report the three order-save outcomes at the tender pane`; tests "a new save replaces queued state and survives a stale acknowledgement" and "rejection is final, idempotent by fields, and can be cleared"; code: "if (current?.kind === 'queued-offline' && current.mutationId === mutationId)" in `packages/core/src/screens/main/pos/checkout/checkout-mode.ts:90` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: present at the read, missed by it)_
18. Render the no-order state before mounting document-dependent checkout hooks when the resource is null — a missing order must not be dereferenced as a checkout document — evidence: `a5f25083c9 2026-07-14 fix(core): modal/detail reads move off legacy storeDB onto the engine database (e-increment 1) (#651)`; test "renders not-found before accessing document observables when the resource emits null"; code: "if (!order) {" in `packages/core/src/screens/main/pos/checkout/checkout.tsx:47` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: present at the read, missed by it)_
19. Enter wide-screen checkout mode in an effect while rendering the columns redirect — writing the external checkout store during render would notify subscribers mid-render — evidence: `c28f97b32b 2026-09-07 fix(checkout): review round on the column swap`; code: "if (redirectToColumns) enterCheckout(orderId);" in `packages/core/src/screens/main/pos/checkout/index.tsx:21` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: present at the read, missed by it)_
20. Await a store-local completion-journal entry after the binding/session gates and before returning a completing sale to its money caller, refusing preparation if the journal cannot be written — a payment must not start without its recoverable finish recorded — evidence: `9794b95466 2026-09-18 feat(checkout): a completing sale is journaled before money moves and replayed at store-session start`; tests "awaits the journal before returning preparation to the money caller" and "refuses preparation if the journal cannot be written"; code: "await recordCompletionAttempt(ctx.storeDB, {" in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:70` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
21. Replace an order’s journal facts and counters for a new attempt while preserving other orders and guarding stale replay mutations by the recorded timestamp — a late resolve or failure must not erase or age a replacement attempt — evidence: `2e57db54f8 2026-09-18 fix(checkout): third review round on the completion journal — a new attempt replaces the entry, journal failures reach the gateway checkout's error path (Codex review)`; `aebc71f681 2026-09-18 fix(checkout): fourth review round on the completion journal — a stale replay cannot mutate a replacement attempt (Codex review)`; code: "if (expectAt !== undefined && data.pending[orderUuid]?.at !== expectAt) return data;" in `packages/core/src/screens/main/pos/checkout/completion-journal.ts:49` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
22. Replay outstanding finishes sequentially at store-session/service start, reusing the run across context changes and StrictMode reconnects and stopping stale lookups — recovery must not collect or push money, replay twice on rerender, or select another sale’s receipt — evidence: `0d2f07f29a 2026-09-18 fix(checkout): second review round on the completion journal — decide an unpaid replay only on the third start, a thrown finish always leaves an entry, replay on service start version (Codex review)`; tests "stays sequential across context changes and StrictMode effect restarts, without replaying twice" and "replays again when the service starts with the same database and runtime"; code: "Replays outstanding finishes at store-session start, never collecting or pushing money." in `packages/core/src/screens/main/pos/checkout/completion-journal-bridge.tsx:23` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
23. Use resident paid status for replay, accepting on-hold/custom paid statuses but not an absent status or the unpaid blocklist, and make one bounded targeted refresh before deciding an unpaid server order — catch-up failures retain the entry, and a successful catch-up must not trigger a second refresh in the finish — evidence: `f40126eaab 2026-09-18 fix(checkout): review round on the completion journal — refresh before deciding an unpaid replay, count missing starts separately, paid-status predicate, recorded actor, audit persistence stated best-effort (Codex review)`; code: "return !!payload?.status && !UNPAID_STATUSES.includes(payload.status);" in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:158`; test "catches up an open resident before replay and never requests a second refresh" — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
24. Count missing-resident and confirmed-unpaid replay starts separately from finish/transport failures, abandon a missing order with a warning only on its third missing start, clear an unpaid attempt only on its third unpaid start, and clear cancellation immediately without completion — intervening pulls need time to reveal payment, while failed finishes must not consume the missing-order allowance — evidence: `0d2f07f29a 2026-09-18 fix(checkout): second review round on the completion journal — decide an unpaid replay only on the third start, a thrown finish always leaves an entry, replay on service start version (Codex review)`; tests "does not abandon the first missing lookup after two finish failures" and "clears a cancelled contract attempt without completing, receipt, or audit"; code: "if (cancelled || count >= 3) await resolveCompletionAttempt(storeDB, uuid, attempt.at);" in `packages/core/src/screens/main/pos/checkout/completion-journal-bridge.tsx:118` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
25. Retain or recreate a thrown finish’s journal entry with its source, actor and last error, propagate the original failure, and replay with the recorded actor rather than the new cashier — successful finishing clears the entry after a best-effort audit call, not an awaitable or exactly-once audit write — evidence: `0d2f07f29a 2026-09-18 fix(checkout): second review round on the completion journal — decide an unpaid replay only on the third start, a thrown finish always leaves an entry, replay on service start version (Codex review)`; test "recreates a concurrently cleared attempt when finishing throws, with source and actor"; code: "{ ...current.ctx, actor: attempt.actor }," in `packages/core/src/screens/main/pos/checkout/completion-journal-bridge.tsx:124`; code: "Audit persistence is best-effort, not at-least-once: the logger exposes no awaitable write." in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:277` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
26. Trust a usable finite decimal server balance for manual completion, falling back to the local prediction only without usable server evidence and never after mirror failure; refresh a still-unsettled server order only when its balance disagrees with the till — server money must remain visible without adding a request to an ordinary split leg — evidence: `c6791fe5e0 2026-09-18 fix(checkout): the manual completion predicate trusts the server summary when one exists (ADR 0032)`; `70babe4a3b 2026-09-18 fix(checkout): review round on the server-summary predicate — show the server balance, journal a server-settled partial leg, validate the balance (Codex review)`; code: "Only when the store disagrees with the till: an ordinary split leg costs no request." in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:194` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
27. Journal a manual leg that the server unexpectedly settles in full before finishing it, without recording a second attempt for an already-prepared completion — the choose-register gate cannot run retroactively after money moved, so missing attribution is reported instead — evidence: `70babe4a3b 2026-09-18 fix(checkout): review round on the server-summary predicate — show the server balance, journal a server-settled partial leg, validate the balance (Codex review)`; test "journals server settlement before finish without recording twice (prepared %s)"; code: "The choose-register gate cannot run retroactively; reportProvenanceGap reports missing attribution." in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:268` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
28. Reattribute only an online unpaid gateway retry to the current session while preserving its existing counter/time/register identity; retain the first session for tender, completing statuses or any captured leg — the sale identity stays fixed while unpaid gateway money belongs to the session where it will land — evidence: `5dcff27682 2026-09-18 fix(checkout): the legacy gateway tab recovers when the register opens; a gateway retry re-attributes an unpaid order to the current session (Codex review)`; tests "%s retry in B replaces only session A" and "captured leg retains session A even before completing status"; code: "Tender legs already took money in their first session; only unpaid gateways can move." in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:90` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
29. Present a session-required refusal as translated open-register-first guidance in an informational toast — the cashier needs the action that enables checkout rather than a raw session failure — evidence: `3aba13863d 2026-09-18 feat(checkout): gateway sales require an open register session when sessions are on`; code: "logger.info(t('pos_checkout.open_register_first'), { showToast: true, context });" in `packages/core/src/screens/main/pos/checkout/session-required.ts:10` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
30. Extends line 25: Mark a recovered completion's audit row as replayed so recovery stays distinguishable from a live completion — evidence: `9794b95466 2026-09-18 feat(checkout): a completing sale is journaled before money moves and replayed at store-session start`, code: `"...(outcome.source === 'replay' ? { replayed: true } : {}),"` in `packages/core/src/screens/main/pos/checkout/sale-completion.ts:249` — platform: all. _(added 2026-09-18 after seeding by wcpos/roadmap#350: landed 2026-09-18 after the source ledger's read)_
