# Behaviour ledger: `pos/checkout/components`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Present the legacy payment frame, reconcile its completion signals and display the payable total.

**Composes:** `@wcpos/components/{error-boundary,text,webview}`.

## Lines

1. Construct the token-bearing payment URL only when a payment link exists — early renders must not throw on `new URL(undefined)` — evidence: `a444829d32 2026-02-06 Fix checkout-to-receipt transition crashes and lost links` — platform: all.
2. Persist provenance before exposing an online pay page; a failed preparation remains blocked even after connectivity drops, and a required register choice blocks exposure — a completing gateway must not outrun its sale stamp — evidence: `payment-webview.tsx:148–150`, “Online, the pay page is exposed only once this order's provenance is persisted” / “(the effect below), and a failed save keeps it closed even if connectivity then” / “drops”; `38a2b32684 2026-09-14 fix(checkout): report the provenance gap only once a sale completes, gate every completing path, thread the store id` — platform: all.
3. Validate received snapshots as plain objects with a positive finite ID, nonblank status and valid POS UUID metadata — malformed bridge data must not be adopted as an order — evidence: `c555903f70 2026-09-01 fix(core): harden payment reconciliation` — platform: all.
4. Treat `pos-open`, `pos-partial`, `pending`, `failed` and `cancelled` as unpaid, rather than allowlisting paid statuses — `on-hold` and intentional custom gateway statuses must still complete — evidence: `58df188925 2026-09-02 fix(checkout): paid-status check is a blocklist of unpaid statuses, and the postMessage handler honours it too` — platform: all.
5. Reject an early unpaid “payment received” message as completion and poll after invalid snapshots — older received pages can speak before asynchronous provider confirmation — evidence: `payment-webview.tsx:449–451`, “before the provider confirms, with the order still unpaid. Don't” / “complete on the message's say-so: leave the poll armed and let” / “server truth decide.” — platform: all.
6. Read fallback payment status directly from the server’s include-read, not merely an engine requirement becoming ready — dirty/coalesced resident tasks can settle without updating this document — evidence: `payment-webview.tsx:332–337`, “The decision reads SERVER truth directly”; “so it cannot prove” / “payment state.” — platform: all.
7. Start load-triggered fallback checks only on subsequent frame navigations; recheck unpaid server truth every three seconds for at most two minutes — initial-page polling races ahead of payment, while one unpaid response can precede a later webhook — evidence: `payment-webview.tsx:545–550`, “The first load is the initial order-pay page, where a payment cannot have” / “completed yet”; `fe74ae3265 2026-09-02 fix(review): keep polling server truth after an early unpaid answer, bounded` — platform: all.
8. Log fallback-query failures at debug rather than as gateway failures — a premature or failed safety-net read previously emitted payment errors for successful sales — evidence: `payment-webview.tsx:393–394`, “expected and must NOT be surfaced as a payment-gateway failure —” / “doing so produced spurious PY02001 errors on successful checkouts.” — platform: all.
9. Retire the paid current order and route to receipt/cart before awaiting local catch-up; recheck completion after awaited reads — slow reconciliation must neither strand the paid sale nor complete it twice — evidence: `2895daeef8 2026-08-29 fix(checkout): retire paid order before routing`; `2231ac78b9 2026-08-29 fix(checkout): open the receipt as soon as the payment is received; never wait on the local refresh` — platform: all; documented failure on iOS.
10. Adopt the received snapshot locally and use targeted refresh as catch-up, bounded to ten seconds with handle release — receipt correctness should improve without making navigation depend on an indefinitely queued requirement — evidence: `52d9e4f4d7 2026-09-01 fix(checkout): adopt the payment-received order snapshot into the local store`; `payment-webview.tsx:265–267`, “Bounded, because a require's” / “`ready` is not guaranteed to settle” — platform: all.
11. Watch only the first payment document: after ninety seconds silently remount once, then offer Retry; key the error boundary with the frame and run no watchdog without a link — later redirects may already follow a successful payment and must not reopen its pay page — evidence: `8a4960175f 2026-09-03 fix(checkout): retry only the first payment document; boundary-keyed remount; no watchdog without a link`; `6c80413e88 2026-09-03 fix(checkout): a stalled payment frame reloads once, then offers Retry` — platform: all; motivated by iOS WebKit stalls.
12. Close readiness synchronously when URL/frame identity changes and on native navigation start — a stale ready gate can post into a replacement document; cross-origin web iframes still lack the navigation-start signal — evidence: `payment-webview.tsx:632–636`, “a passive effect would leave the gate reporting `ready`” / “against the replacement document until after paint”; `payment-webview.tsx:584–586`, “Native only: the web WebView renders an `<iframe>`, which exposes no” / “navigation-start event for a cross-origin document.” — platform: all; stated residual gap on web/Electron.
13. Display the net payment total after refunds rather than the original order total — the checkout headline must agree with the amount Pay uses — evidence: `c853e1b2af 2026-03-30 fix: address review feedback from chatgpt-codex-connector, coderabbitai` — platform: all.
