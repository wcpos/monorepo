# Behaviour ledger: `components/product`

Seeded 2026-09-18 from `.claude/research/2026-09-18-composed-behaviour-ledger.md` on `research/composed-ledger` (wcpos/roadmap#341) by wcpos/roadmap#345. Numbers are assigned once and never reused: a struck line leaves a gap, a new line takes the next number. Every line keeps its evidence. The rules for preserving or striking a line are in the [library strategy](https://github.com/wcpos/roadmap/blob/worktree-docs%2Bdesign-program-2026-09-12/docs/design/2026-09-18-library-strategy.md), section 3. Not reworded from the source.

**Job:** Shared product cells, taxonomy filters/selectors, stock and price interpretation, images, tax information, and expandable variation tables.

**Composes:** `@wcpos/components/button`, `/combobox`, `/error-boundary`, `/hover-card`, `/hstack`, `/icon`, `/icon-button`, `/image`, `/select`, `/suspense`, `/table`, `/text`, `/tooltip`, `/tree-combobox`, `/virtualized-list`, `/vstack`; utility `/lib/utils`; type-only `/lib/use-hierarchy` and `/combobox/types`.

## Lines

1. Product/variation cells read selected engine-record fields rather than constructing document-field observables on every render — retains live field updates without the old fresh-observable subscription pattern — evidence: `8fb046cd19 2026-08-17 refactor(core,query)!: products/variations onto engine-native records (ADR 0028 step 2b)` — platform: all.
2. Taxonomy badges apply ID-array filters, while all six filter pills read and mutate shared query state — selecting a category in a row must also update the filter bar, not leave independent local state — evidence: `d0afbf5c24 2026-03-26 fix: derive category pill state from query instead of local state`; `4d7dd2a020 2026-07-15 feat(core): the products screen rides the query-state store + bindings (migration 7/8) (#660)` — platform: all.
3. Category filtering uses a hierarchical multi-select and loads tree options only when its content mounts — preserves parent/child category selection without eagerly loading the tree for every closed control — evidence: `254447a933 2026-03-26 feat: composable TreeCombobox with category tree filtering` — platform: all.
4. Categories without IDs are excluded before building tree options — prevents unusable identities entering hierarchical selection — evidence: `dcb42bcacf 2026-03-27 fix: filter out categories with missing IDs before building tree options` — platform: all.
5. `CategorySelect` accepts and forwards a controlled value — edit/add forms must display their selected category instead of the placeholder — evidence: `53e4812ccd 2026-03-25 fix: address review feedback from chatgpt-codex-connector, coderabbitai` — platform: all.
6. The category pill resolves selected labels independently of the lazily loaded option tree, uses loading/fallback text, and condenses multiple names to the first plus a count — selected IDs must remain intelligible before opening the menu or after a refresh — evidence: `b06513b3b2 2026-06-05 fix: resolve filter pill id regressions`; test “keeps a safe fallback until a refreshed category emits its real name” — platform: all.
7. `CategoryTreeLoader` creates its binding above its own Suspense boundary and places only the reader below — otherwise every suspended render discards and recreates the resource indefinitely — evidence: `a2e12d5bb6 2026-08-30 refactor(core): fix the Suspense retry loop by moving boundaries, not by caching`; test “hands its options over on the first emission, having subscribed exactly once” — platform: all.
8. Category-tree callers supply a stable options callback (`setOptions`) — an unstable callback would retrigger the effect and render indefinitely — evidence: “callback will cause infinite re-renders via the useEffect dependency.” at `components/product/category-select.tsx:89` — platform: all.
9. Taxonomy selections convert string option values back to numeric filter IDs — combobox identities and query identities use different representations — evidence: “we need to convert the value to a number because the value is a string” at `components/product/filter-bar/brands-pill.tsx:29` and `tag-pill.tsx:29` — platform: all.
10. Clearing stock status supplies `{ value: '', label: '' }` rather than `undefined` to Select — `undefined` would retain the previous selected value — evidence: “it won't clear the previous value, so we need to make sure we return { value: '', label: '' }” at `components/product/filter-bar/stock-status-pill.tsx:29` — platform: all.
11. Category/tag/brand search lists extend their binding window at end-reached through a shared guard — restores access beyond the first 50 results while avoiding repeated extensions on short pages — evidence: `948d888601 2026-08-25 fix(core): the search-select pickers page again (#1553)` — platform: all.
12. Product attributes, taxonomy labels, and grouped names decode HTML entities — WooCommerce names must render as merchant-facing text rather than encoded strings — evidence: `e135b15fe4 2024-12-13 add decodeHtml prop to text component`; “Sometimes the product name from WooCommerce is encoded in html entities” at `components/product/grouped-names.tsx:29` — platform: all.
13. Variation-option labels are decoded without changing option values — the visible text may change, but matching must retain the catalogue's original identity string — evidence: `0266c5971c 2026-08-25 fix(ui): decode HTML entities across server-supplied name surfaces`; “Decode the LABEL only — `value` is the identity sent back to” at `components/product/variation-select.tsx:77` — platform: all.
14. Recycled attribute/taxonomy/variation children use index keys rather than product-specific keys — preserves the documented FlashList recycling convention — evidence: “@NOTE - Don't use a unique key here, index is sufficient” at `components/product/attributes.tsx:25`, `tags.tsx:28`, and `variable-product-row/variations/table.tsx:86` — platform: all.
15. Active product/variation filter pills use an undefined default variant and inactive pills use muted — preserves the active icon-color correction — evidence: `83f564f5f0 2025-03-03 fix pill icon colours` — platform: all.
16. Variation attributes use a Select for at most ten options and a searchable Combobox for longer lists — keeps short choices direct while making large option sets searchable — evidence: “Select for short list of options” at `components/product/variation-select.tsx:52` and “Combobox for longer list of options” at `variation-select.tsx:88`; `9e02d0c987 2025-07-10 Fix Variation options combobox` — platform: all.
17. Grouped names are queried using the parent's finite remote-ID set, with a limit large enough for that set — unrelated products or the ordinary first page must not determine the displayed group — evidence: `8769a678e3 2026-07-15 feat(core): POS products rides the query-state store + bindings (migration 8/8 — the last screen) (#661)`; test “reads grouped products through a finite-id collection binding” — platform: all.
18. Every variable row owns isolated variation query state, and unmounting its collapsed table clears search and attribute filters — one row's selections must not leak into another row or survive reopening — evidence: `8769a678e3 2026-07-15 feat(core): POS products rides the query-state store + bindings (migration 8/8 — the last screen) (#661)`; tests “owns isolated variations query state for every variable-product row” and “clears a row filter when its collapsed variations table unmounts” — platform: all.
19. Expanded variations default to ascending `menu_order` rather than name — preserves the intended merchant-defined variation order — evidence: `75aecfc659 2026-07-30 feat(query): variations default-sort by menu_order with Woo id tiebreak (#871)` — platform: all.
20. Opening an expanded variation table requests one non-blocking refresh, not another refresh on every render — resident records remain visible while reopening still checks server freshness — evidence: `8a749dd281 2026-08-05 fix(core): refresh a parent's variations once per picker open (#990)`; test “refreshes variations once when expanded, not when re-rendered” — platform: all.
21. The variation binding follows changes to the parent's variation-ID list — children added or removed by a later parent update must change the bound set — evidence: `28b6ca6d38 2026-08-05 fix(core): keep variation binding ids reactive`; test “updates the bound variation ids when the parent observable changes” — platform: all.
22. Expanding a relational-search hit seeds the row's variation search from the parent search term and clears old attribute matches — expansion should reveal the children responsible for the match — evidence: “when relational search found child variations, seed this row's query state” at `components/product/attributes.tsx:101`, followed by “with the parent search term before mounting its variations table.” at line 102 — platform: all.
23. Clicking an attribute option expands the row, clears its search, and replaces only that attribute's match; match identity includes both ID and name — explicit attribute selection must not remain constrained by an old search or overwrite another same-ID attribute — evidence: `8769a678e3 2026-07-15 feat(core): POS products rides the query-state store + bindings (migration 8/8 — the last screen) (#661)`; test “resolves the option for the matching attribute identity” — platform: all.
24. Attribute links, variable images, and collapse controls use table-meta `setRowExpanded` instead of TanStack's updater — computed-property destructuring in the original updater broke collapse in minified production builds — evidence: `65e0cc3cc7 2026-01-23 fix: bypass TanStack Table minification bug and migrate to scheduleOnRN` — platform: all.
25. Expanded-state observables and variation-context values are explicitly memoized — subscription/context identity must not churn merely because their parent renders, including where compiler memoization is disabled — evidence: `f11b2e3d70 2026-08-16 perf(products): memoise the expanded-row observable in VariableProductRow (#1246)`; `d9e3aa3612 2026-08-16 perf(core): memoise observables built during render instead of rebuilding them per render (#1241)` — platform: all.
26. `VariableProductRow`, `Variations`, and `VariationsTable` all retain `'use no memo'` — stable TanStack row identities otherwise let React Compiler cache stale visible columns — evidence: `948e339158 2026-08-14 fix(products): apply column changes to variable/variation rows immediately`, body names #1228 and facebook/react#33057; test “drops hidden columns from the parent row and variation subrows immediately” — platform: all.
27. A non-scrolling ScrollView measures variation content inside the animated-height container — native children otherwise lay out at height zero and cannot provide the expansion height — evidence: `8b9283bf4e 2025-07-09 Fix: variations show/hide on native`; “On native, the Variations will be rendered with height 0.” at `components/product/variable-product-row/index.tsx:131` — platform: iOS/Android.
28. Variation content mounts for expansion and remains mounted until a completed collapse animation; worklet callbacks schedule React state changes onto RN — permits height measurement and visible collapse before teardown — evidence: “Handle mounting when expanding” at `components/product/variable-product-row/index.tsx:77` and “Handle unmounting after collapsing” at line 89; `65e0cc3cc7 2026-01-23 fix: bypass TanStack Table minification bug and migrate to scheduleOnRN` — platform: all.
29. Variable-row cells have individual Suspense boundaries — an image resource can suspend without withholding the whole row's other cells — evidence: `4f2d370975 2026-06-11 refactor(core): load image attachments via suspense resources, not effects` — platform: web/Electron for the documented image-loading defect; boundaries exist on all.
30. Synthetic variation rows return the actual parent Row from `getParentRow()`, while carrying the variation hit as their own data — returning the parent's data object made variation Add to Cart silently no-op — evidence: `8d3c1ad4e9 2026-02-13 fix: repair variation add-to-cart broken by strict mode migration` — platform: all.
31. Variation footer totals use the reactive parent variation count, preserve authoritative zero, and floor that count at rendered rows; absent parent lists defer to nullable binding totals — avoids “3 of 3” for twenty server variations, stale-cache totals, and “2 of 0”; unknown totals show the count alone — evidence: `2300c98bde 2026-08-05 fix(core): variations footer total from the parent's server variation ids (#989)`; `08b9439a9b 2026-08-22 fix(variations): keep an authoritative zero from the parent payload`, body names #1492; `f93565dcbd 2026-08-22 fix(query): never print a denominator nothing vouches for` — platform: all.
32. Variation “clear and refresh” uses guarded local collection reset, then sync, never durable server deletes — the former refresh action could delete the merchant's variations and discard pending work — evidence: `8349439fdf 2026-08-08 fix(core)!: variations clear-and-refresh evicts locally, server deletes get a choke point (#1093) (#1097)`; test “clear-and-refresh resets the local variations collection and never writes to the engine” — platform: all.
33. Sync activity uses `useObservableEagerState` — synchronous activity emissions during render previously caused cross-component setState warnings — evidence: `3459a4aca9 2026-03-03 fix: use useObservableEagerState for active$ to prevent setState-during-render warning` — platform: all.
34. Stock resolution derives self-managed stock from finite quantity and backorder permission; parent-managed or unmanaged stock follows the server flag — parent-managed variations must not be judged using their own irrelevant quantity — evidence: “When the record manages its own stock, quantity + backorders decide” at `components/product/resolve-stock.ts:31`, and “(including parent-managed stock) the stock_status flag governs.” at line 33 — platform: all.
35. Backorders are permitted only for explicit `'yes'` or `'notify'`, through a shared predicate — a missing/unknown field at zero quantity must not authorize a sale that WooCommerce would refuse — evidence: `74e8c7488d 2026-08-20 fix(core): missing backorders field sells out at zero, matching WooCommerce`; `809f88f755 2026-08-20 fix(core): cart oversell guard uses the shared backorders allow-list` — platform: all.
36. Display stock status reflects local quantity edits immediately, but only derives over built-in status vocabulary; custom and parent-managed statuses remain server-owned — avoids waiting for an acknowledgement offline without overwriting plugin statuses such as `lowstock` — evidence: `c8f0bcaeb2 2026-08-19 fix(core): stock badge derives its status locally instead of waiting for the server echo`; `3e10109844 2026-08-20 fix(core): derive stock status only over the built-in vocabulary` — platform: all.
37. Expanded variations obey the live Stock Status pill, not a second hidden display setting; clearing it admits every variation, and filtering tracks local quantity edits — clearing the product filter previously still hid sixteen of twenty variations — evidence: `8d1221cc98 2026-08-25 fix(pos): variation lists follow the Stock Status pill, not the display setting`; test “shows every variation when the Stock Status filter is cleared” — platform: all.
38. Variation names are composed from sanitized, nonempty attribute values, with served-name fallback only when none survive — server titles collapsed every variation with three or more attributes to the same parent name — evidence: `d0a4e6b58a 2026-08-25 fix(pos): compose the variation row name from its own attributes (#1589)`; tests “recovers a collapsed 1.10.0 name from the attributes” and “never throws on malformed entries, and never renders undefined” — platform: all.
39. Variation images prefer a usable `images[0]`, fall back to singular `image`, and show the local placeholder on load errors — different server lanes deliver different image shapes; singular-only reads blanked thumbnails — evidence: `afe4f1a290 2026-08-25 fix(products): variation thumbnails read the v2 images array (#1577) (#1578)`; test “falls back to `image` when `images` is present but empty — an emptied gallery is not a URL” — platform: all.
40. The shared image resolver can return the complete image entry, including attachment ID, rather than only its URL — cart-line consumers need the variation's own server-matchable image, not a persisted parent-image fallback — evidence: `f18d84a9af 2026-08-25 fix(pos): the cart line takes the variation's own image, not the parent's`, body names #1577 — platform: all.
41. Product-image fallback is an inline, precomputed base64 SVG — it works offline without the dead external placeholder host, and avoids Android's broken handling of percent-encoded SVG data URIs — evidence: `9088989a28 2026-07-15 fix(product): variable-price object payloads + offline image placeholder (#670)`; “expo-image on Android routes every” / “`data:` URI through a base64 decoder” at `components/product/product-image-placeholder.ts:5–6` — platform: all; encoding requirement specifically Android.
42. Variable-price metadata accepts typed objects and legacy JSON strings — object payloads must not be treated as JSON parse failures — evidence: `9088989a28 2026-07-15 fix(product): variable-price object payloads + offline image placeholder (#670)` — platform: all.
43. Variable-price validation accepts omitted sub-ranges, requires valid string min/max values for present ranges, treats literal null as no priced variations, and rejects omitted/malformed values; missing display ranges render nothing — prevents valid no-sale products losing their price while still reporting malformed metadata — evidence: `311b907b6a 2026-07-31 fix(core): accept omitted sub-ranges in variable-prices meta`; `9d4001b6cb 2026-07-31 fix(core): distinguish omitted variable price metadata`; `aeb9ae25da 2026-03-25 fix: address review feedback for variable price display` — platform: all.
44. An absent variable-price metadata key is quiet only when all three served parent price fields are blank; unread fields are not evidence of blankness — distinguishes legitimate unpriced variations from augmentation failure — evidence: `a52405894f 2026-08-25 fix(product): name the product on PRODUCT421, and stop firing it on a legitimate state`; test “reports when the caller read no price fields — absence is not evidence of blankness” — platform: all.
45. Variable-price errors include record ID, server product ID, name, and SKU when available — cashiers need an identifiable product, and repeated-log collapsing must not merge faults from different products — evidence: `a52405894f 2026-08-25 fix(product): name the product on PRODUCT421, and stop firing it on a legitimate state` — platform: all.
46. Tax display reads settings and location through their separate contexts — unrelated cart writes should not invalidate settings-only consumers, while the tax-basis display still follows the customer address — evidence: `037f17a72c 2026-08-16 fix(pos): split the tax-rates context so cart writes stop re-rendering the products panel (#1239)` — platform: all.
47. Tax-rate names are HTML-decoded in the diagnostic table — merchant-authored names such as encoded “VAT & Duty” need readable display — evidence: `0266c5971c 2026-08-25 fix(ui): decode HTML entities across server-supplied name surfaces` — platform: all.
48. Tax-basis text is restricted to one line in both normal and no-rates states — narrow POS panels previously wrapped the footer label one character per line — evidence: `f4b4179aec 2026-09-05 fix(ui): narrow-column wrapping in the POS footer and the Store health database table` — platform: all.
49. Filter pills have field-bearing test IDs, and variation rows expose remote-ID-bearing selectors — tests must identify which filter or variation changed without localized text or generic row counts — evidence: `3fcc2758bf 2026-08-23 test(e2e): cover the product category filter, and make the filter bar addressable`; “Id-bearing so a test can name WHICH variation rendered.” at `components/product/variable-product-row/variations/table.tsx:97` — platform: all.
50. Product-status selection uses `OptionSelect` with width matching and canonical option forwarding — preserves selected labels across platforms instead of exposing web primitives' raw-value labels — evidence: `254447a933 2026-03-26 feat: composable TreeCombobox with category tree filtering`; `ccceebc667 2026-08-23 refactor(components): consolidate Select call sites behind OptionSelect (#1499)` — platform: all; label correction specifically web/Electron.
