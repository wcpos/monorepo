{
  "name": "@woo-rxdb-lab/sync-engine-rxdb",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/sync-engine-rxdb/src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@woo-rxdb-lab/shared": "workspace:*",
    "@woo-rxdb-lab/sync-core": "workspace:*",
    "rxdb": "17.2.0"
  },
  "devDependencies": {
    "rxdb-premium": "17.2.0"
  }
}
