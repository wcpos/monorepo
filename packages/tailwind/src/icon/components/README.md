React Native components are generated during development from raw SVG files using `@svgr/cli`. 
This is done in preference to transpiling using a babel plugin or similar (which causes more headaches).