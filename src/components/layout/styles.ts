import styled from 'styled-components/native';

export const Wrapper = styled.View`
	display: flex;
	flex-direction: column;
	height: 100%;
`;

export const Masterbar = styled.View`
	flex: 0 1 auto;
`;

export const Main = styled.View`
	flex: 1;
`;
