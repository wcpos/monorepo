import styled from 'styled-components/native';

type Props = { theme: import('../../lib/theme/types').ThemeProps } & import('./view').Props;

export const Wrapper = styled.View<Props>``;

export const StyledPopoverView = styled.View<Props>``;
