// import { init } from './variations.helpers';

const { init } = require('./variations.helpers').init;

describe('Variations Helpers', () => {
	it('init state', () => {
		expect(true).toBeTruthy();
	});
});
