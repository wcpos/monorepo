import React from 'react';
import Button from '../../../../../components/button';

interface Props {
	product: any;
}

const Actions = ({ product }: Props) => {
	return <Button title="Show" />;
};

export default Actions;
