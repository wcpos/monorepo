import React from 'react';
import { View, Text } from 'react-native';

interface Props {}

const Support: React.FC<Props> = ({ header, main, title }) => {
	return <Text>Support</Text>;
};

export default Support;
