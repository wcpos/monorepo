import styled from 'styled-components/native';
import { ThemeProps } from '../../lib/theme/types';

export const SplashView = styled.View<{ theme: ThemeProps }>`
	width: 100%;
	align-items: center;
	justify-content: center;
`;
