import type { ProductSchema, ProductDocument, ProductCollection } from './products';

export type { ProductSchema, ProductDocument, ProductCollection };
export { products as default } from './products';
