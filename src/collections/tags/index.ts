import type { ProductTagSchema, ProductTagDocument, ProductTagCollection } from './tags';

export type { ProductTagSchema, ProductTagDocument, ProductTagCollection };
export { tags as default } from './tags';
